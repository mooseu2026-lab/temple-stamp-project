# 챕터 2 — 14. 확인포인트 9종 삼중검증 (Postman v2.1 · Workbench SQL · curl) — [예제 17]

> 작성일 2026-09-05 · 의견04(컨트롤러 4종 반영 보고, 테스트 52건) 반영판
> 이 카드가 끝나면 챕터 2가 닫힌다. 다음은 챕터 3(카카오 검색 + 관리자 사찰·코스 등록).

---

## 0. 의견04에 대한 결정 — 2026-09-05 확정. §5 지시문 STEP 1이 이 표를 그대로 실행한다

| # | 질의 | 결정안 | 근거 |
|---|---|---|---|
| 4 | 미리보기(`/page`)가 `phrase_seen`을 기록해 문구 풀을 소진 | **기록 시점을 도장 완료(3단계 COMPLETED)로 옮긴다.** `/page`는 기록 없는 조회(seen에 없는 것 우선 정렬은 유지), `StampService` 완료 처리에서 `markPhraseSeen` 호출 | 명세의 "같은 권역 안에서 재출력 금지"는 **순례 중 실제로 받은 문구**에 대한 규칙이다. 새로고침 5번이 5편을 태우면 규칙이 목적을 잃는다. 미션과 정책이 같아져 §3-1 비대칭도 절반 해소 |
| 3-1 | 문구 없음=500, 미션 없음=null 비대칭 | **유지.** 문구는 500 SITE-5001, 미션 미리보기는 null | 미션 부재는 QR 통과 시 `VERSE-4041`로 반드시 드러난다(조용히 넘어갈 길이 없음). 문구 부재는 어디서도 안 드러나므로 500이 유일한 경보. 둘 다 "누락이 반드시 드러난다"는 같은 원칙의 결과 |
| 3-2 | 원고 편집 중 일시적 500 감수 | **감수한다. 대신 관리자 저장을 "삭제 후 재삽입"이 아닌 UPSERT(`INSERT ... ON DUPLICATE KEY UPDATE`)로 강제** | 조합이 잠깐 비는 순간 자체를 없애면 경고 로그 모드가 필요 없다. 챕터 3 AdminContentController 규칙에 넣는다 |
| 6-③ | `GET /api/courses/{id}/progress` 유지 여부 | **삭제(`[AUDIT]` 처리 후 챕터 4에서 제거).** 진행률은 이미 목록·상세·미션 결과 세 곳에 `progress`로 실린다 | 컨트롤러가 직접 `AUTH-4013`을 던지는 저장소 유일의 자리 = "필터는 기록, Config는 판단, EntryPoint가 401 작성" 원칙 위반. 프론트가 아직 없으므로 지금이 제일 싸다 |
| 5 | `/api/regions/**` vs 정확 일치 | **정확 일치로 좁힌다.** 하위 경로 계획 없음 | 명세 §4에 `/api/regions/{id}`가 없다(옛 `regions/{id}` 형식은 폐기) |
| 2 | `target=AGE99` → COMMON-4003 | **교재 주석 수정 완료**(챕터2-12 파일 반영) | 타입 변환 실패와 검증 실패는 다른 예외 |

이 결정으로 **확인포인트 ②가 바뀐다**: 로그인 후 `/page` 2회 → `phrase_seen` 은 **0 유지**가 정답이 된다(문구 변화는 시드가 계층당 1편이라 관찰 불가 — 챕터 5 도장 완료 후에 `phrase_seen=1`로 검증).

---

## 1. 검증 세트의 구조

```
postman/
├── temple-stamp.postman_collection.json     ← 9종 + 사전 2건(가입·로그인). Tests 탭에 자동 판정
├── local.postman_environment.json           ← baseUrl · accessToken · 계정
└── run.sh                                   ← newman 으로 컬렉션 실행 (Postman GUI 없이 터미널에서)
docs/verify/ch2-checkpoints.sql              ← Workbench 에서 눈으로 확인하는 9개 SQL
docs/verify/ch2-checkpoints.sh               ← curl 9줄 (가장 빠른 스모크)
```

세 겹인 이유: Postman은 **프론트가 보는 그대로**(헤더·본문·코드), SQL은 **DB에 실제로 남은 것**(seen 기록·건수), curl은 **CI/배포 직후 30초 스모크**. 하나가 통과해도 나머지가 깨질 수 있는 지점(예: 200인데 seen이 기록됨)을 서로 잡는다.

**공통 Tests 스크립트**(모든 요청에 붙는다):
```javascript
pm.test("X-Request-Id 존재", () => pm.response.to.have.header("X-Request-Id"));
pm.test("timestamp +09:00", () => {
    const j = pm.response.json();
    pm.expect(j.timestamp).to.match(/\+09:00$/);
});
```
`/health` 만 예외 — Actuator 응답은 `ApiResponse` 로 감싸지 않으므로 timestamp 검사에서 제외한다.

---

## 2. Postman 컬렉션 v2.1 전문

**파일** `backend/postman/temple-stamp.postman_collection.json`

```json
{
  "info": {
    "name": "temple-stamp ch2 — 마스터 조회 확인포인트 9종",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.request.url.getPath().endsWith('/health')) {",
          "  pm.test('X-Request-Id 존재', () => pm.response.to.have.header('X-Request-Id'));",
          "  pm.test('timestamp +09:00', () => pm.expect(pm.response.json().timestamp).to.match(/\\+09:00$/));",
          "}"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "00 사전 — 회원가입 (이미 있으면 409 AUTH-4090 도 통과)",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('201 또는 409', () => pm.expect([201, 409]).to.include(pm.response.code));"
      ]}}],
      "request": {
        "method": "POST",
        "header": [{ "key": "Content-Type", "value": "application/json" }],
        "url": { "raw": "{{baseUrl}}/api/auth/signup", "host": ["{{baseUrl}}"], "path": ["api", "auth", "signup"] },
        "body": { "mode": "raw", "raw": "{\"email\":\"{{email}}\",\"password\":\"{{password}}\",\"nickname\":\"확인포인트\"}" }
      }
    },
    {
      "name": "00 사전 — 로그인 → accessToken 저장",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('200', () => pm.response.to.have.status(200));",
        "const d = pm.response.json().data;",
        "pm.expect(d.tokenType).to.eql('Bearer');",
        "pm.expect(d.expiresIn).to.eql(1800);",
        "pm.environment.set('accessToken', d.accessToken);",
        "pm.environment.set('userId', d.user.userId);"
      ]}}],
      "request": {
        "method": "POST",
        "header": [{ "key": "Content-Type", "value": "application/json" }],
        "url": { "raw": "{{baseUrl}}/api/auth/login", "host": ["{{baseUrl}}"], "path": ["api", "auth", "login"] },
        "body": { "mode": "raw", "raw": "{\"email\":\"{{email}}\",\"password\":\"{{password}}\"}" }
      }
    },
    {
      "name": "① 비로그인 GET /api/courses → progress null",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('200 success', () => { pm.response.to.have.status(200); pm.expect(pm.response.json().success).to.be.true; });",
        "const items = pm.response.json().data.items;",
        "pm.test('items 1건 이상', () => pm.expect(items.length).to.be.above(0));",
        "pm.test('모든 progress 가 null', () => items.forEach(c => pm.expect(c.progress).to.be.null));"
      ]}}],
      "request": { "method": "GET", "header": [],
        "url": { "raw": "{{baseUrl}}/api/courses", "host": ["{{baseUrl}}"], "path": ["api", "courses"] } }
    },
    {
      "name": "② 로그인 GET /api/sites/1/page 1회차",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('200', () => pm.response.to.have.status(200));",
        "const d = pm.response.json().data;",
        "pm.test('8블록 키 존재', () => ['site','verse','expansionPhrase','mission','viewpoints','badges','riderInfo','verifyState'].forEach(k => pm.expect(d).to.have.property(k)));",
        "pm.test('guideAvailable 없음', () => pm.expect(d).to.not.have.property('guideAvailable'));",
        "pm.test('미시작이라 verifyState null', () => pm.expect(d.verifyState).to.be.null);",
        "pm.environment.set('phraseId1', d.expansionPhrase.expansionPhraseId);"
      ]}}],
      "request": { "method": "GET",
        "header": [{ "key": "Authorization", "value": "Bearer {{accessToken}}" }],
        "url": { "raw": "{{baseUrl}}/api/sites/1/page", "host": ["{{baseUrl}}"], "path": ["api", "sites", "1", "page"] } }
    },
    {
      "name": "② 로그인 GET /api/sites/1/page 2회차 (seen 미기록 확인은 SQL 로)",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('200', () => pm.response.to.have.status(200));",
        "const d = pm.response.json().data;",
        "pm.test('문구 블록 존재(계층당 1편 시드라 동일 문구 허용)', () => pm.expect(d.expansionPhrase.expansionPhraseId).to.be.a('number'));"
      ]}}],
      "request": { "method": "GET",
        "header": [{ "key": "Authorization", "value": "Bearer {{accessToken}}" }],
        "url": { "raw": "{{baseUrl}}/api/sites/1/page", "host": ["{{baseUrl}}"], "path": ["api", "sites", "1", "page"] } }
    },
    {
      "name": "③ GET /api/courses?regionId=0 → 400 COMMON-4000",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('400', () => pm.response.to.have.status(400));",
        "const e = pm.response.json();",
        "pm.test('success false, code COMMON-4000', () => { pm.expect(e.success).to.be.false; pm.expect(e.error.code).to.eql('COMMON-4000'); });",
        "pm.test('fields 에 regionId', () => pm.expect(JSON.stringify(e.error.fields)).to.include('regionId'));"
      ]}}],
      "request": { "method": "GET", "header": [],
        "url": { "raw": "{{baseUrl}}/api/courses?regionId=0", "host": ["{{baseUrl}}"], "path": ["api", "courses"],
                 "query": [{ "key": "regionId", "value": "0" }] } }
    },
    {
      "name": "④ GET /api/courses/1 + Accept-Language en-US → siteName 영문",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('200', () => pm.response.to.have.status(200));",
        "const d = pm.response.json().data;",
        "pm.test('sites 5건', () => pm.expect(d.sites.length).to.eql(5));",
        "pm.test('첫 사찰 이름이 영문', () => pm.expect(d.sites[0].siteName).to.match(/^[A-Za-z]/));",
        "pm.test('progress 있음(로그인)', () => pm.expect(d.progress === null || typeof d.progress === 'object').to.be.true);"
      ]}}],
      "request": { "method": "GET",
        "header": [{ "key": "Accept-Language", "value": "en-US,en;q=0.9" }, { "key": "Authorization", "value": "Bearer {{accessToken}}" }],
        "url": { "raw": "{{baseUrl}}/api/courses/1", "host": ["{{baseUrl}}"], "path": ["api", "courses", "1"] } }
    },
    {
      "name": "⑤ GET /api/sites/1/guide → steps 7칸",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('200', () => pm.response.to.have.status(200));",
        "const s = pm.response.json().data.steps;",
        "pm.test('항상 7칸', () => pm.expect(s.length).to.eql(7));",
        "pm.test('sortNo 1~7 순서', () => s.forEach((x, i) => pm.expect(x.sortNo).to.eql(i + 1)));",
        "pm.test('없는 자리는 etiquette null + passage 존재', () => s.filter(x => !x.present).forEach(x => { pm.expect(x.etiquette).to.be.null; pm.expect(x.passage).to.be.a('string'); }));"
      ]}}],
      "request": { "method": "GET", "header": [],
        "url": { "raw": "{{baseUrl}}/api/sites/1/guide", "host": ["{{baseUrl}}"], "path": ["api", "sites", "1", "guide"] } }
    },
    {
      "name": "⑥-a GET /api/verses/99 → 400 COMMON-4000",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('400 COMMON-4000', () => { pm.response.to.have.status(400); pm.expect(pm.response.json().error.code).to.eql('COMMON-4000'); });"
      ]}}],
      "request": { "method": "GET", "header": [],
        "url": { "raw": "{{baseUrl}}/api/verses/99", "host": ["{{baseUrl}}"], "path": ["api", "verses", "99"] } }
    },
    {
      "name": "⑥-b GET /api/sites/999 → 404 SITE-4040",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('404 SITE-4040', () => { pm.response.to.have.status(404); pm.expect(pm.response.json().error.code).to.eql('SITE-4040'); });"
      ]}}],
      "request": { "method": "GET", "header": [],
        "url": { "raw": "{{baseUrl}}/api/sites/999", "host": ["{{baseUrl}}"], "path": ["api", "sites", "999"] } }
    },
    {
      "name": "⑥-c GET /api/sites/1/page?target=AGE99 → 400 COMMON-4003",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('400 COMMON-4003 (enum 변환 실패)', () => { pm.response.to.have.status(400); pm.expect(pm.response.json().error.code).to.eql('COMMON-4003'); });"
      ]}}],
      "request": { "method": "GET", "header": [],
        "url": { "raw": "{{baseUrl}}/api/sites/1/page?target=AGE99", "host": ["{{baseUrl}}"], "path": ["api", "sites", "1", "page"],
                 "query": [{ "key": "target", "value": "AGE99" }] } }
    },
    {
      "name": "⑦ 좌표 포함 POST /api/pilgrimages (로그인) → 400 COMMON-4001",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('400 COMMON-4001 — CoordinateFieldGuard 가 컨트롤러 앞에서 끊음', () => { pm.response.to.have.status(400); pm.expect(pm.response.json().error.code).to.eql('COMMON-4001'); });"
      ]}}],
      "request": {
        "method": "POST",
        "header": [{ "key": "Content-Type", "value": "application/json" }, { "key": "Authorization", "value": "Bearer {{accessToken}}" }],
        "url": { "raw": "{{baseUrl}}/api/pilgrimages", "host": ["{{baseUrl}}"], "path": ["api", "pilgrimages"] },
        "body": { "mode": "raw", "raw": "{\"courseId\":1,\"latitude\":37.57,\"longitude\":126.98}" }
      }
    },
    {
      "name": "⑧ 응답 헤더·timestamp (공통 Tests 가 전 요청에서 검사 — 여기서는 regions 로 재확인)",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('200, items 9건', () => { pm.response.to.have.status(200); pm.expect(pm.response.json().data.items.length).to.eql(9); });",
        "pm.test('X-Request-Id 8자리', () => pm.expect(pm.response.headers.get('X-Request-Id')).to.have.lengthOf(8));"
      ]}}],
      "request": { "method": "GET", "header": [],
        "url": { "raw": "{{baseUrl}}/api/regions", "host": ["{{baseUrl}}"], "path": ["api", "regions"] } }
    },
    {
      "name": "⑨ GET /health → {status:UP}",
      "event": [{ "listen": "test", "script": { "type": "text/javascript", "exec": [
        "pm.test('200 UP', () => { pm.response.to.have.status(200); pm.expect(pm.response.json().status).to.eql('UP'); });",
        "pm.test('ApiResponse 로 감싸지 않음(Actuator 원형)', () => pm.expect(pm.response.json()).to.not.have.property('success'));"
      ]}}],
      "request": { "method": "GET", "header": [],
        "url": { "raw": "{{baseUrl}}/health", "host": ["{{baseUrl}}"], "path": ["health"] } }
    }
  ]
}
```

**파일** `backend/postman/local.postman_environment.json`
```json
{
  "name": "temple-stamp local",
  "values": [
    { "key": "baseUrl",     "value": "http://localhost:8080", "enabled": true },
    { "key": "email",       "value": "checkpoint@test.com",    "enabled": true },
    { "key": "password",    "value": "Check!2026",             "enabled": true },
    { "key": "accessToken", "value": "",                       "enabled": true },
    { "key": "userId",      "value": "",                       "enabled": true },
    { "key": "phraseId1",   "value": "",                       "enabled": true }
  ]
}
```

**파일** `backend/postman/run.sh` — Postman GUI 없이 실행
```bash
#!/usr/bin/env bash
# newman = Postman 컬렉션 CLI 러너. npx 가 처음 한 번 내려받는다 (npm 레지스트리 접근 필요)
cd "$(dirname "$0")"
npx --yes newman run temple-stamp.postman_collection.json \
  -e local.postman_environment.json \
  --reporters cli,json --reporter-json-export ../docs/verify/ch2-newman-result.json
```

**왜 이렇게 하는가**
- ⑦을 로그인 상태로 보내는 이유: `springSecurityFilterChain`은 order -100이라 order 1~3의 우리 필터보다 **먼저** 돈다. 비로그인으로 보내면 CoordinateFieldGuard 에 닿기 전에 401 이 난다. 좌표 차단을 검증하려면 토큰이 있어야 한다.
- 로그인 응답의 `expiresIn == 1800`을 여기서 다시 확인하는 이유: 토큰 수명 표기(1시간·14일 폐기)가 문서마다 달랐던 이력이 있어, 코드가 확정값을 내는지 매 실행마다 못박는다.

---

## 3. Workbench SQL — DB 에 실제로 남은 것

**파일** `backend/docs/verify/ch2-checkpoints.sql`
```sql
-- backend/docs/verify/ch2-checkpoints.sql
-- Postman 을 한 번 돌린 직후 Workbench 에서 위에서 아래로 실행. 기대값은 각 줄 오른쪽 주석
USE temple_stamp_project;

-- ① 비로그인 조회는 이력을 남기지 않는다                                   기대: 0
SELECT COUNT(*) AS anon_seen FROM phrase_seen WHERE user_id NOT IN (SELECT user_id FROM users);

-- ② 결정안 §0-4 적용 후: 미리보기는 기록하지 않는다                          기대: 0
--    (적용 전이라면 2 이상. 값이 2면 아직 옛 동작 — STEP 1 미반영)
SELECT COUNT(*) AS preview_seen
  FROM phrase_seen ps JOIN users u ON u.user_id = ps.user_id
 WHERE u.email = 'checkpoint@test.com';

-- ②' 확인포인트 계정의 계층·언어 (page 가 무엇을 기준으로 골랐는지 역추적)     기대: tier NULL(→AGE30), locale ko
SELECT user_id, tier, locale FROM users WHERE email = 'checkpoint@test.com';

-- ③ 검증 실패는 DB 에 닿지 않는다 — 확인할 것이 없음. 대신 region 이 정말 9건인지
SELECT COUNT(*) AS regions FROM region;                                       -- 9

-- ④ en 번역이 있는 사찰                                                     기대: site 1 이 en 행을 가짐
SELECT s.site_id, s.name AS ko, i.name AS en
  FROM site s LEFT JOIN site_i18n i ON i.site_id = s.site_id AND i.locale = 'en'
 WHERE s.site_id IN (SELECT site_id FROM course_site WHERE course_id = 1)
 ORDER BY (SELECT position FROM course_site cs WHERE cs.site_id = s.site_id);

-- ⑤ 가는 법 R1 — ON 조건이 맞으면 어떤 site_id 를 넣어도 정확히 7행           기대: 7 / 7
SELECT COUNT(*) AS steps_site1
  FROM shrine_element e
  LEFT JOIN site_element se ON se.element_id = e.element_id AND se.site_id = 1;
SELECT COUNT(*) AS steps_site999
  FROM shrine_element e
  LEFT JOIN site_element se ON se.element_id = e.element_id AND se.site_id = 999;  -- 없는 절이어도 7 (사전이 7행이므로)

-- ⑥ 정의역 밖 구절은 애초에 없다                                            기대: verse_no 1~5 만
SELECT verse_no FROM gwan_verse ORDER BY verse_no;

-- ⑦ 좌표 컬럼이 사용자 테이블 어디에도 없다 (원칙①)                          기대: 0행
SELECT TABLE_NAME, COLUMN_NAME
  FROM information_schema.COLUMNS
 WHERE TABLE_SCHEMA = 'temple_stamp_project'
   AND COLUMN_NAME IN ('latitude','longitude','lat','lng')
   AND TABLE_NAME NOT IN ('site');                                          -- site 는 시설 고정 좌표라 예외

-- ⑧ 요청ID·timestamp 는 DB 에 없다 — 확인 대상 아님

-- ⑨ health 가 UP 이면 커넥션이 살아 있다 — 같은 커넥션에서 한 줄
SELECT NOW() AS db_alive, @@time_zone AS tz;                                   -- tz 가 SYSTEM 이면 서버 TZ(Asia/Seoul) 확인
```

---

## 4. curl 스모크 — 30초

**파일** `backend/docs/verify/ch2-checkpoints.sh`
```bash
#!/usr/bin/env bash
# backend/docs/verify/ch2-checkpoints.sh — 기동 직후 30초 스모크. 실패하면 줄 번호를 찍고 종료(set -e)
set -euo pipefail
B=${BASE_URL:-http://localhost:8080}
code() { curl -s -o /dev/null -w "%{http_code}" "$@"; }        # 상태코드만
expect() { [ "$1" == "$2" ] && echo "OK   $3" || { echo "FAIL $3 (got $1, want $2)"; exit 1; }; }

# 사전 — 로그인 (없으면 가입)
curl -s -X POST "$B/api/auth/signup" -H 'Content-Type: application/json' \
  -d '{"email":"checkpoint@test.com","password":"Check!2026","nickname":"확인포인트"}' > /dev/null || true
TOKEN=$(curl -s -X POST "$B/api/auth/login" -H 'Content-Type: application/json' \
  -d '{"email":"checkpoint@test.com","password":"Check!2026"}' | jq -r '.data.accessToken')
[ "${#TOKEN}" -gt 20 ] || { echo "FAIL login"; exit 1; }

expect "$(curl -s "$B/api/courses" | jq -r '.data.items[0].progress')"                     "null" "① 비로그인 progress null"
expect "$(code -H "Authorization: Bearer $TOKEN" "$B/api/sites/1/page")"                    "200"  "② 로그인 page 200"
expect "$(code "$B/api/courses?regionId=0")"                                                "400"  "③ regionId=0 → 400"
expect "$(curl -s -H 'Accept-Language: en-US' "$B/api/courses/1" | jq -r '.data.sites[0].siteName' | grep -c '^[A-Za-z]')" "1" "④ en siteName"
expect "$(curl -s "$B/api/sites/1/guide" | jq '.data.steps|length')"                        "7"    "⑤ guide 7칸"
expect "$(code "$B/api/verses/99")"                                                         "400"  "⑥-a verses/99 → 400"
expect "$(code "$B/api/sites/999")"                                                         "404"  "⑥-b sites/999 → 404"
expect "$(curl -s "$B/api/sites/1/page?target=AGE99" | jq -r '.error.code')"                "COMMON-4003" "⑥-c target=AGE99"
expect "$(curl -s -X POST "$B/api/pilgrimages" -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
          -d '{"courseId":1,"latitude":37.5}' | jq -r '.error.code')"                        "COMMON-4001" "⑦ 좌표 차단"
expect "$(curl -sI "$B/api/regions" | grep -ci '^x-request-id')"                             "1"    "⑧ X-Request-Id"
expect "$(curl -s "$B/api/regions" | jq -r '.timestamp' | grep -c '+09:00$')"                "1"    "⑧ timestamp +09:00"
expect "$(curl -s "$B/health" | jq -r '.status')"                                            "UP"   "⑨ health UP"
echo "ALL 12 OK"
```

---

## 5. Claude Code 지시문 — 확정본. 이 블록만 그대로 붙여넣는다

```
temple-stamp-project/backend 에서 작업한다. 스택 고정: Spring Boot 3.5.16 · Java 21 · Lombok · MyBatis 3.0.5 · MySQL 8 · jjwt 0.13.0. 응답 DTO 는 record, Row 는 Lombok 클래스. 기존 파일은 append 우선, 수정은 아래에 명시된 곳만. 브랜치 feature/ch2-checkpoints 를 develop 에서 만든다. 교재 원문은 docs/textbook/ch2-14.md 다 — §2·§3·§4 의 파일 내용은 한 글자도 바꾸지 말고 그대로 쓴다.

STEP 1 — 의견04 결정 반영 (§0 확정)
 a. phrase_seen 기록 시점 이동. SitePageService 는 "seen 에 없는 것 우선 → RAND()" 정렬 조회만 하고 INSERT IGNORE phrase_seen 을 호출하지 않는다. 기록 호출 자체는 챕터 5(스탬프 3단계 COMPLETED 전이)에서 넣을 것이므로 이번에는 제거만 한다. 완료 시점에 "어느 문구를 봤는지"를 서버가 알 방법(stamp 행에 저장 vs MissionSubmitRequest 에 선택 필드 expansionPhraseId 추가)은 구현하지 말고 두 안의 장단만 docs/audit/ch2-checkpoints.md 에 적는다. SitePageParamIntegrationTest 에 seen 증가를 기대하는 테스트가 있으면 "0 유지" 로 바꾼다.
 b. SecurityConfig [course] 구역: GET "/api/regions/**" → GET "/api/regions" 정확 일치로 좁힌다. 이 한 줄 외에는 손대지 않는다.
 c. CourseController.progress 메서드와 그것만을 위해 존재하는 Service/Mapper 메서드에 주석 `// [AUDIT] 명세 §4 에 없음 — 챕터 4 에서 제거 예정 (진행률은 list/detail/mission 응답에 포함)` 를 단다. 삭제하지 않는다.
 d. 문구 없음=500 SITE-5001 / 미션 미리보기 없음=null 비대칭은 유지. SitePageService 클래스 javadoc 에 이유를 3줄로 적는다: 미션 부재는 QR 통과 시 VERSE-4041 로 반드시 드러남 / 문구 부재는 어디서도 드러나지 않아 500 이 유일한 경보 / 둘 다 "누락은 반드시 드러난다"는 같은 원칙.
 e. 관리자 문구·미션 저장은 챕터 3 에서 UPSERT(INSERT ... ON DUPLICATE KEY UPDATE) 로 강제할 예정 — ExpansionPhraseMapper.xml·MissionMapper.xml 상단에 `<!-- [RULE] 관리자 저장은 UPSERT 만. 삭제 후 재삽입 금지 — 조합이 잠깐 비면 SITE-5001 -->` 주석만 미리 단다.

STEP 2 — 검증 세트 파일 생성 (ch2-14.md §2·§3·§4 원문 그대로)
 postman/temple-stamp.postman_collection.json
 postman/local.postman_environment.json
 postman/run.sh
 docs/verify/ch2-checkpoints.sql
 docs/verify/ch2-checkpoints.sh
 sh 두 개는 chmod +x. run.sh 의 --reporter-json-export 경로(../docs/verify/)가 실제로 존재하는지 확인한다.

STEP 3 — 실행 (순서 고정)
 1) ./gradlew test — 52건 이상 통과. 실패하면 STEP 1 의 변경 때문인지 먼저 확인.
 2) ./gradlew bootRun 을 백그라운드로 띄우고 /health 가 UP 이 될 때까지 최대 60초 대기.
 3) bash docs/verify/ch2-checkpoints.sh — 마지막 줄 ALL 12 OK.
 4) bash postman/run.sh — 실패 0건. npx newman 이 네트워크로 막히면 그 사실만 적고 3)·5) 결과로 대신한다.
 5) MySQL 로 docs/verify/ch2-checkpoints.sql 을 실행하고 결과 전체를 docs/verify/ch2-sql-result.txt 로 저장. 기대값: ② preview_seen = 0, ⑤ 7/7, ⑦ 0행, regions 9.
 기대값과 다른 것이 하나라도 있으면: 원인 → 고친 것 → 재실행 결과를 docs/audit/ch2-checkpoints.md 에 적는다. 명세·에러코드·스키마를 바꾸는 수정은 절대 하지 말고 [질문] 으로 남긴다.

STEP 4 — 보고서 docs/audit/ch2-checkpoints.md
 순서대로: ① STEP 1 a~e 변경 파일 목록 ② seen 기록 위치 두 안의 장단 ③ 테스트 건수 ④ ch2-checkpoints.sh 출력 12줄 원문 ⑤ newman 요약(또는 미실행 사유) ⑥ SQL 기대값 대조표 ⑦ [질문] 목록. 마지막 줄에 curl 결과(ALL 12 OK 여부)를 다시 한 번 적는다.

STEP 5 — 커밋·PR
 [site] 미리보기 seen 기록 제거 / [global] regions 경로 정확 일치 / [verify] 검증 세트 5파일 — 세 커밋으로 나눈다. develop 으로 PR 을 열고 본문에 ch2-checkpoints.md 전문을 붙인다. 리뷰어 mooseu2026-lab. main 에는 절대 직접 푸시하지 않는다. bootRun 프로세스는 마지막에 종료한다.
```

**예성 직접 (3가지)**: ① 이 파일을 `backend/docs/textbook/ch2-14.md` 로 저장 ② 위 블록을 Claude Code 에 붙여넣기 ③ 끝나면 `docs/audit/ch2-checkpoints.md` 마지막 줄이 `ALL 12 OK` 인지 확인. **Chrome 담당**: 없음.

---

## 6. 챕터 2 완료 기준

- [ ] `ch2-checkpoints.sh` → `ALL 12 OK`
- [ ] newman 실패 0건(또는 네트워크 사유 기록)
- [ ] SQL ②가 0 (미리보기 seen 미기록), ⑤가 7/7, ⑦이 0행
- [ ] `docs/audit/ch2-checkpoints.md` 존재, `[AUDIT]` progress 표기
- [ ] 노션 「1.권역코스사찰구절 마스터조회」에 12·13·14 카드 게시 — 이 부분은 **Chrome 에 위임 가능**: "노션 페이지 X 에 마크다운 파일 Y 를 붙여넣어 하위 페이지 12·13·14 생성"

다음: **챕터 3 — 카카오 장소 검색 + 관리자 사찰·코스 등록** (KakaoMapService·AdminSiteController·AdminCourseController·UPSERT 규칙). 예성 직접 작업 1건: 카카오 개발자 콘솔에서 REST API 키 발급(Chrome 이 화면까지는 안내).
