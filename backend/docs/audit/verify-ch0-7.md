# 전체 점검 D — 챕터 0~7(+보강) 실측

정본 `backend/docs/textbook/verify-ch0-7.md` · 2026-09-06 · 전제: 챕터 7 보강 ⚠ 0 확인 후 시작
코드·스키마는 고치지 않았다. 고친 것은 **점검 도구뿐**이고, 수정 전 파일은 `docs/audit/verify-d-toolcheck/` 에 있다.

---

## ① 도구 자가검증 — 4/4 "잡힘" (둘은 고친 뒤)

일부러 결함을 심고 도구가 잡는지 본 다음 원복했다. **먼저 이것부터 한 이유**는 챕터 7에서
cleanup·mapper-check·H12 셋이 조용히 거짓 통과한 적이 있어서다.

| # | 도구 | 심은 결함 | 처음 결과 | 조치 | 최종 |
|---|---|---|---|---|---|
| 1 | `cleanup.sql` + `run-all.sh` | FK 로 막히는 `DELETE FROM site WHERE site_id = 1` | **못 잡음** — 경고만 찍고 종료코드 0 으로 끝까지 진행. mysql stderr 는 `2>/dev/null` 로 버려지고 있었다 | stderr 를 보여 주고 `exit 1` | **잡힘** (`ERROR 1451` 원문 출력 + 종료코드 1) |
| 2 | `mapper-check.js` | XML 에만 있는 id 1개 · Java 에만 있는 메서드 1개 | 잡음 | — | **잡힘** (양방향 2건) |
| 3 | `db-check.sh` H12 | 복제할 원본 0행 | ✅ 로 세지는 않았으나(➖) 결과만 보면 통과로 읽혔다 | "실측하지 못했다 — 통과로 셀 수 없다" 경고 줄 추가 | **잡힘** |
| 4 | `endpoint-scan.js` | 컨트롤러에 임시 메서드 1개 | 잡음 | — | **잡힘** (76 → 77) |

원복 후 diff 0: `cleanup.sql` · `mapper-check.js` · `CertificateController.java` · `endpoint-scan.js`.
1·3 은 도구를 고쳤으므로 diff 가 의도적으로 다르며, 수정 전 파일이 `verify-d-toolcheck/` 에 남아 있다.

**본 점검 중에 도구 결함을 하나 더 잡았다(⑧에 다시 적는다).** `endpoint-scan.js` 가 `SecurityConfig` 의
GET permitAll 블록을 통째로 놓치고 있었다 — 매처 목록 사이의 주석에 괄호가 있어(`(→ 401)`) 정규식이 거기서 끊겼다.
그 결과 공개 조회 13개가 "보호 경로" 로 잘못 적혀 있었고, **비로그인 실측**을 돌려서야 드러났다.

---

## ② 엔드포인트

```
엔드포인트 76 · ✅ 74 · ⚠ 2(그중 ch8/9 2) · ❌ 0
```

**ch8/9 를 뺀 ⚠ 는 0** 이다. 전체 목록은 `docs/audit/endpoints.md`(메서드 경로 | 인증 | newman 요청 id | 판정).
미검증 둘은 `GET`·`DELETE /api/print-orders/{id}` — 인쇄 주문이 있어야 부를 수 있어 챕터 9 몫이다.

### 비로그인 허용 목록 — 선언 ↔ 실측

`SecurityConfig` 에서 뽑은 목록과 **토큰 없이 76개를 전부 두드린 결과**가 일치한다(어긋남 0).

| 매처 | 메서드 | 왜 열려 있나 |
|---|---|---|
| `/api/auth/**` | ALL | 토큰을 얻는 문이라 열려 있어야 한다 |
| `/error` | ALL | 스프링 기본 오류 경로 |
| `/api/regions` | GET | 하위 경로가 생길 때 함께 열리지 않도록 정확히 일치 |
| `/api/courses/**` `/api/sites/**` `/api/verses/**` `/api/meditations/**` `/api/content/**` | GET | 조회는 누구나 — 쓰기는 이 목록 밖이라 로그인이 필요하다 |
| `/api/certificates/verify/**` | GET | 제3자 진위 확인 |
| `/health` | GET | 헬스체크. 막으면 배포가 401 을 받는다 |
| `/**` | OPTIONS | CORS preflight 전용 |

```
비로그인 실측 76개 — 허용 17 · 보호(401) 59 · 어긋남 0
```

`POST /api/auth/refresh` 는 쿠키가 없으면 401 을 주지만 그것은 **컨트롤러가 준 401**(`AUTH-4014`)이다.
필터가 막는 401(`AUTH-4013`)과 구분해서 센다 — 안 그러면 열린 경로가 닫힌 것으로 보인다.

---

## ③ JUnit — 187 / 187 (실제 MySQL, 실패 0 · skip 0 · 클래스 22)

동시성은 여섯 건이 돈다. 교재가 요구한 넷(관리자 승인 2 · 다짐 제출 2)이 그 안에 있다.

| 무엇을 동시에 | 어디 | 결과 |
|---|---|---|
| 다짐 제출 ↔ **관리자 승인** | `StampLockOrderIntegrationTest` | 교착 0 · 둘 다 200 |
| **관리자 승인 계열** 완주 재집계 ×2 사용자 | `CompletionServiceTest` | 교착 0 |
| **다짐 제출** — 두 사람이 같은 코스 마지막 칸 (5회 반복) | `MissionConcurrencyIntegrationTest` | 교착 0 · 각자 완주 1 · 인증서 1 · 번호 중복 0 |
| **다짐 제출** — 한 사람이 두 자리 (5회 반복) | `MissionConcurrencyIntegrationTest` | 교착 0 · 인증서는 많아야 한 장 |
| 인증서 동시 발행 10건 | `CertificateServiceTest` | 번호 중복 0 |
| 같은 코스 동시 시작 | `PilgrimageIntegrationTest` | 순례 1건 |

---

## ④ newman — 2회 완전 일치

| | 요청 | 단언 | 실패 |
|---|---|---|---|
| 본 실행(A·U·P·M·G·K·S·C·T·V·R·W·X) | 315 | 1438 | 0 |
| 회향(준비 SQL 뒤 별도 컬렉션) | 32 | 168 | 0 |
| **합** | **347** | **1606** | **0** |

1회차와 2회차의 요청 수·단언 수·**SQL 채점 파일이 완전히 같다(diff 0)**.
채점에 `NOW()` 를 넣었더니 매 실행 달라져 diff 가 0이 되지 않았다 — 그 값은 H6 이 이미 보므로 채점에서는 뺐다.

SQL 채점(발췌): 잠금 5회·`locked` 1 · 재사용 뒤 살아 있는 refresh 0 · 미리보기 `phrase_seen` 0 ·
i18n UPSERT 2행 · 뷰포인트 1행 · 이동시간 20행 · 멱등 순례 1건 · 사진 1행·문장 1행·`is_edited` 1 ·
W 완주 0·유효 인증서 0·회수 3장 · 보상 `GRANTED 5 / PAID 1(needs_review 1) / REVOKED 1` ·
번호 중복 0 · 코스 원복 0 · 배송 정보 1행 · 본체에 주소 컬럼 0.

---

## ⑤ 런타임 · 보안

**런타임** — `ERROR 0 · 스택트레이스 0 · 5xx 0 · WARN 743 중 허용목록 밖 0`.
매퍼 정합은 양방향 0 · `#{}`↔`@Param` 0 · 별칭 미채움 0(select 77개).

**보안 S1~S18 — 통과 32 · 실패 0.**

| S | 내용 | 근거 |
|---|---|---|
| S1~S4 | 무인증 401 · USER→ADMIN 403 · 남의 자원 4xx(본문 유출 0) · 좌표 차단 | X 폴더 66요청 |
| S5 | 액세스 1800초 · refresh 는 SHA-256 64자로만 저장 · HttpOnly·SameSite · prod Secure | 설정·SQL·Set-Cookie |
| S6 | JWT ≠ QR · `.gitignore` · 로그·소스에 시크릿 0 | grep |
| S7 | 전 계정 BCrypt · 해시 길이 60 | SQL |
| S8~S10 | 업로드 키 소유·경로 조작 400 · `/env`·`/actuator` 401 · 인젝션 문자열이 값으로만 | X 폴더 |
| S11 | 오리진에 `*` 없음 · 허용 밖 preflight 에 Allow-Origin 0개 | 실측 |
| S12 | 위치동의 없으면 GPS 403 · 닉네임 마스킹 · 전자책은 `is_private = 0` 만 | 코드·SQL |
| **S13** | 비로그인 허용 목록 = 선언과 실측 일치(어긋남 0) | `open-endpoint-probe.js` 76개 |
| **S14** | 신설 ADMIN 3개 — 토큰 없으면 401, USER 토큰이면 403 (6건) | 실측 |
| **S15** | verify 응답 선언에 `userId·email·phone·address` 없음(필드 7개 고정) | 선언 검사 |
| **S16** | 사용자 보상 질의·DTO 에 배송 정보 없음 / 관리자 심사 질의에만 있음 | XML·DTO |
| **S17** | `/api/thinkbox`·`/api/meditations/logs`·`/api/pilgrimages` 에 좌표를 실으면 전부 400 | 음성 테스트 3건 |
| **S18** | 폐기된 refresh 재사용 → 401 이고 그 사용자의 살아 있는 토큰 0 | 실측 + SQL |

---

## ⑥ DB H1~H14 · 시드 · 시간대 · 커넥션

| H | 결과 |
|---|---|
| H1 | `/health` 200 `{"status":"UP"}` · DB 왕복 1 |
| H2 | newman 전후 `Threads_connected` 13 → 13 (풀 크기 이내) |
| H3 | **10분을 실제로 기다린 뒤** `/api/regions`·`/api/courses/1`·`/health` 전부 200 |
| H4 | `INNODB_TRX` 0행 |
| H5 | 교착 기록 없음 · 잠금 대기 0 |
| H6 | `global/session time_zone = SYSTEM`, `NOW()` 가 호스트 KST 와 일치 |
| H7 | 외래키 고아 4종 전부 0 |
| H8 | `utf8mb4 / utf8mb4_unicode_ci` · 한글 왕복 그대로 |
| H9 | 표 **33** · 사찰 110 · 코스 12 · `slot_site` 218 · 요소 342 · region 10 |
| H10 | JUnit 동시성(위 ③) |
| H11 | 챕터 6 표 고아 0 · 사찰별 DIRECT 문장 중복 0 |
| H12 | 유니크 3종 **실측** — 중복 INSERT 가 셋 다 1062 로 막혔다 |
| **H13** | 챕터 7 표·유니크·FK·생성 컬럼 — **차이 0**(표 5 · 유니크 7 · FK 2 · `valid_pilgrimage_id` 가 `STORED GENERATED` 인 것까지) |
| **H14** | 정리.md §5-1 ALTER 8건 **누락 0** · 옛 `uk_certificate_pilgrimage` 가 지워진 것도 확인 |

**시드 무결성**(정리 뒤에도 불변): 사찰 110 · 코스 12(전부 DRAFT) · 자리 60 · 후보 218(시드 213 + 데모 5) ·
참배요소 342 · region 10. **좌표 (0,0) 사찰 1곳**(봉인사 — 카카오 장소 DB 에 없다).
**인증서 번호 중복 0 · 재사용 0.** `@@global.time_zone = SYSTEM`(배포 체크리스트 §5-2 9번 대조 대상).

시드 요소 채점이 처음에 343 으로 나왔다 — 채점은 정리보다 **앞에서** 돌기 때문에(§6-5) 검증 사찰의 요소가 얹힌 값이었다.
채점 질의에서 `검증사찰%` 를 빼서 시드 무결성이 검증 데이터에 흔들리지 않게 했다(도구 수정).

**커넥션 풀 스냅샷**(2회차 중간): `Threads_connected 13` · `PROCESSLIST` 는 전부 `Sleep 12`(대기 중인 질의 0).
HikariCP 는 `maximumPoolSize` 를 따로 설정하지 않아 기본 10이고, 기동 로그의 `HikariPool-1 - Start completed.` 뒤로
풀 관련 경고가 없다.

---

## ⑦ 기능 F01~F33

**✅ 33 · ⚠ 0 · ❌ 0** (2026-09-06 반영 — F14 는 JUnit 실측 근거로 ✅, F04 는 챕터 1 보강으로 구현)

| F | 기능 | 판정 | 근거 / 원인 |
|---|---|---|---|
| F01 | 회원가입·검증·동의 | ✅ | A01~A03 · U05~U08c |
| F02 | 로그인 30분·refresh 회전·재사용 탐지 | ✅ | A04~A12 · S5 · S18 |
| F03 | 5회 실패 잠금 403 | ✅ | A07·A08 · 채점 `login_fail_count 5` |
| F04 | 내 정보·**탈퇴(연쇄)** | ✅ | 내 정보 U01~U04 · **탈퇴는 챕터 1 보강에서 구현**(U-D1~U-D6 · JUnit 10건 · `audit/ch1-withdraw.md`). 2026-09-06 이후 판정 |
| F05 | 권역 10행(제주 준비 중)·코스 목록 ACTIVE만 | ✅ | M01~M03 · 채점 region 10 |
| F06 | 코스 상세·슬롯 5·후보·congested | ✅ | M04~M07·M11~M13 |
| F07 | 싱글페이지 8블록·가는 법 7칸 | ✅ | M08~M10 · M17 |
| F08 | 관리자 사찰 UPSERT·status 필터·좌표 화이트리스트 | ✅ | S01~S12 · V06b · G02 |
| F09 | 카카오 장소 연동 | ✅ | K01~K04 (키가 있어 200) |
| F10 | 순례 시작·여권·진행률 한 곳 | ✅ | P01~P08 · 채점 순례 1건 |
| F11 | GPS 1단계(siteId 필수·좌표 미수신) | ✅ | T02~T04b · S17 |
| F12 | QR 2단계(만료 없음·회전 무효화) | ✅ | T06~T08 · T14a·T14b |
| F13 | 미션·다짐 → 도장 발행 | ✅ | T09·T10 |
| F14 | 세션 60분 만료(expireIfStale + 청소기) | ✅ | JUnit `T-08` 이 실측한다(409 + DB 에 `EXPIRED` 남음). **2026-09-06 결정: 시간 기반 기능은 JUnit 실측을 ✅ 근거로 인정**한다 — newman 은 60분을 기다릴 수 없고, 테스트 전용 "만료 강제" 엔드포인트는 만들지 않는다 |
| F15 | 하루 5·예외접수 2·승인은 한도 무관 | ✅ | T11·T12·T16 · W 폴더에서 5개를 채운 뒤의 승인 4건이 전부 200 |
| F16 | 슬롯 1도장·409·by-slot | ✅ | T11·T11b |
| F17 | 도장 심사(승인·반려·COMPLETED↔REJECTED) | ✅ | T15 · W16·W19 |
| F18 | presign(jpeg/png·키 소유) | ✅ | V01~V03 · R16·R17 |
| F19 | 사진 1 + 문장 1 UPSERT | ✅ | V04~V08b · 채점 1행/1행 |
| F20 | 생각상자·flashback 200+null | ✅ | V09~V15 |
| F21 | 명상 기록 | ✅ | V16~V19 · R05~R07 |
| F22 | 완주 판정·완주 행·재집계 멱등 | ✅ | W01~W03 · W09 · W23~W23c |
| F23 | 관리자 재집계 1명·배치 | ✅ | W23·W24 (`failed:[]`) |
| F24 | 취소 연쇄(인증서 회수·GRANTED만 REVOKED·needs_review) | ✅ | W17·W17b·W17c · 채점 `needs_review 1` |
| F25 | 인증서 자동 발행·시퀀스·중복 0 | ✅ | W04 · 채점 중복 0·재사용 0 |
| F26 | 관리자 회수·재발행 새 번호 | ✅ | W25~W25c · W19b |
| F27 | 공개 진위(비로그인·마스킹·회수 200+REVOKED) | ✅ | W05·W06·W18·W25d · S15 |
| F28 | 보상 적립 멱등·claimable 서버 계산 | ✅ | R08 · W08 · W09 |
| F29 | 수령 신청(배송정보 분리) | ✅ | W26·W11·W26b · 채점 `claim_rows 1`·본체 주소 컬럼 0 |
| F30 | 관리자 보상 심사·needs_review 우선 | ✅ | W14·W20·W20b·W22d |
| F31 | 회향 하한 12·HH 인증서 | ✅ | W15(미성립)·W21(성립) |
| F32 | 재성립 시 보상 같은 행 복귀 | ✅ | W27 |
| F33 | 공통 규약·@Validated 400·404 미노출 | ✅ | X 폴더 66요청 · G 폴더 |

극단값이 아니다(전부 ✅ 도 0건도 아님) — ⚠ 둘은 성격이 서로 다르고 근거가 각각 있다.

---

## ⑧ 제품 결함 / 도구 결함

### 제품 — 2건 (둘 다 이번에 고치지 않았다. 원칙 5)

| # | 무엇 | 성격 |
|---|---|---|
| ~~P-1~~ | ~~회원 탈퇴가 없다~~ | **닫힘(2026-09-06)** — 챕터 1 보강에서 구현. `DELETE /api/users/me`, 연쇄 9단계, 엔드포인트 77 |
| ~~P-2~~ | ~~세션 만료를 newman 이 실측하지 못한다~~ | **닫힘(2026-09-06)** — 시간 기반 기능은 JUnit 실측을 근거로 인정하기로 정했다 |

### 도구 — 3건 (전부 이번에 고쳤다)

| # | 무엇 | 왜 위험했나 | 조치 |
|---|---|---|---|
| T-1 | `run-all.sh` 가 정리 실패를 **경고만 하고 계속** 진행 · mysql stderr 폐기 | 오염된 상태로 낸 숫자를 통과로 읽는다. 챕터 7 에서 실제로 그랬다 | stderr 노출 + `exit 1` |
| T-2 | `endpoint-scan.js` 가 `SecurityConfig` 의 GET permitAll 블록을 **통째로 놓침**(주석 속 괄호에서 정규식이 끊김) | 공개 조회 13개가 "보호 경로" 로 적혀 있었다. 반대 방향으로 틀렸다면 열린 문을 못 봤을 것이다 | 주석 제거 후 매칭 · 비로그인 실측(S13)으로 교차 확인 |
| T-3 | 시드 요소 채점이 **검증 데이터에 흔들림**(342 ↔ 343) | 매 실행 숫자가 달라 "2회 동일" 이 깨진다 | 채점에서 `검증사찰%` 제외 · 채점의 `NOW()` 제거 |

도구 결함이 세 건 나왔다는 사실 자체가 §6 자가검증을 먼저 돌린 이유다.
**T-2 는 자가검증 넷으로는 안 잡혔고, 76개를 실제로 두드려 보고서야 드러났다** — 심어 보는 것만으로는 부족하다.

---

## ⑨ [질문]

| # | 무엇 | 지금 | 정하면 |
|---|---|---|---|
| ~~1~~ | ~~회원 탈퇴~~ | **정해짐 — 만들었다**(챕터 1 보강). 남은 갈래는 `audit/ch1-withdraw.md` §7 | — |
| ~~2~~ | ~~시간 기반 기능의 판정 기준~~ | **정해짐** — JUnit 실측을 ✅ 근거로 인정. 테스트 전용 만료 엔드포인트는 만들지 않는다 | — |
| ~~3~~ | ~~`@@time_zone = SYSTEM`~~ | **정해짐** — 운영·개발 모두 `+09:00` 명시. 하루 한도 자정과 flashback 이 Asia/Seoul 기준이라 OS 에 기대면 안 된다 | — |
| 4 | 미참조 `ErrorCode` 11개 | 명세 코드라 유지 중 | 명세 §7 과 대조해 정리 시점 결정(챕터 8·9 에서 일부 쓰인다) |

---

**챕터 8 진입 조건 대조**: 엔드포인트 ⚠(ch8/9 제외) 0 ✅ · 2회 동일 ✅ · 5xx 0 ✅ · S1~S18 ✅ ·
H1~H14 ✅ · 도구 4/4 ✅ · 기능 ⚠ 는 **회원 탈퇴 하나만** 남았고, 그것은 챕터 1 보강으로 만들기로 정해졌다(`audit/ch1-withdraw.md`).

verify-D: API 77(✅75 ⚠2 ❌0) · 기능 ✅33 ⚠0 ❌0 · newman 361/361×2 · JUnit 197/197 · 도구 4/4
(2026-09-06 결정 반영본. 실측 당시 원본은 ✅31 ⚠2 였고 F14 의 판정 근거만 바뀌었다)
