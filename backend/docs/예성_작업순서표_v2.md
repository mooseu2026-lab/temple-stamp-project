# 예성 작업 순서표 v2 (2026-09-07 · G 진행 중 시점)

폴더 정리 완료 기준. 문서는 전부 `backend/docs/` 아래(`spec/`·`textbook/`·`frontend/`·`audit/`·`verify/`·`정리.md`). 루트 `frontend/`는 비어 있음(FE-1에서 채움).

각 단계는 같은 패턴입니다 — **예성: 파일 넣기(있으면) → 지시문 붙여넣기 → 마지막 줄을 저에게** / **저: 판정 + 다음 정본·지시문**.

---

## 단계표

| # | 단계 | 코드 | 예성이 할 일 | Claude Code가 만드는 파일 | 저에게 보낼 것 | 제가 돌려주는 것 |
|---|---|---|---|---|---|---|
| **1** | 감사 G (진행 중) | 무변경 | 기다리기 | `audit/audit-G-잔여작업_노션용.md` · `postman/예성-손확인.md` | 보강·F·G 마지막 줄 3개 + G 문서 본문 | 배포 진입 판정 · 잔여 항목별 처리 방침 |
| **2** | 감사 H + 프론트 설계 대조 | 무변경 | **지시문 A** 붙여넣기 | `audit/audit-H-명세매칭_노션용.md` · `frontend/frontend-design-actual.md` · `frontend-design-diff.md` · `api-types.d.ts` | H·fe-design 마지막 줄 2개 + H·diff 문서 본문 | ⛔ 항목별 "되돌림/유지" 권고표 → **예성 결정** |
| **3** | 개발용 콘솔 | 추가 | **지시문 B** 붙여넣기 | `src/main/resources/dev-console/*` · `DevConsoleConfig` · `verify/dev-console-remove-check.sh` · `audit/dev-console.md` | 마지막 줄 + 화면 캡처 | 화면 손볼 곳 · "쉽게 읽는 판"(F·G·H 통합, 노션용) |
| **4** | 예성 결정 | — | 2의 권고표에서 ⛔마다 되돌림/유지 선택 | — | 결정 목록 | 챕터 10 정본(되돌릴 것이 있을 때만) + 설계서 v2 |
| **5** | 챕터 10 명세 정합 (조건부) | 고침 | `ch10.md`를 `textbook/`에, 지시문 붙여넣기 | 코드 + `audit/ch10-*.md` + newman 폴더 | 마지막 줄 | 닫힘 판정 |
| **6** | 배포 트랙 | 설정 | `deploy.md`를 `textbook/`에, 지시문 붙여넣기 · **운영 서버·DB·도메인·저장소 정보는 예성이 준비** | `audit/deploy-*.md` · 운영 체크리스트 실측 | 마지막 줄 + prod `/health` 캡처 | 운영 개시 판정 |
| **7** | FE-1 (프론트 1장: 스택·클라이언트·토큰·인트로·로그인) | 프론트 | `frontend/fe1.md`를 넣고, 지시문 붙여넣기 · 카카오 **JavaScript 키** 발급 | 루트 `frontend/` React 프로젝트 · Playwright 결과 | 마지막 줄 + 화면 캡처 | 닫힘 판정. **콘솔 삭제**가 FE-1 닫힘 조건 |
| 8~14 | FE-2 ~ FE-8 | 프론트 | 같은 패턴 반복 | | | |
| 병행 | 콘텐츠 트랙 | 코드 아님 | 원고 140편 CSV(양식은 `verify/body/manuscripts-sample.csv`) · 명상 108선 · 사찰 110곳 QR 위치 힌트 답사 · 봉인사 좌표 · 코스 ACTIVE 전환 시점 | — | CSV dryRun 결과 | — |

5는 2의 결과에 따라 **없을 수도** 있습니다. 6과 7은 순서를 바꿔도 됩니다(백엔드를 먼저 올려 두고 프론트를 붙이는 것이 보통).

---

## 지시문 A (2단계 — G 마지막 줄이 온 뒤)

```
[감사 H → 프론트 설계 대조 연속 실행] 경로 규칙: 정본 문서에 docs/spec/ 이라 적힌 것은 backend/docs/spec/ 으로, docs/frontend/ 는 backend/docs/frontend/ 로 읽는다(저장소에 루트 docs/ 가 없다). 감사 G가 끝났으면 이 순서로 한다. 둘 다 코드 무변경. STEP마다 결과 파일 저장 후 한 줄 보고.

1부. 감사 H: 정본 backend/docs/textbook/spec-match-audit.md §3의 지시문을 그대로 실행한다. 기획 명세 원본은 backend/docs/spec/01~04 네 파일이다(없으면 멈추고 보고). 명세 안의 모순(세션 90분/60분, access 1시간/30분, 경로 temples/sites)은 ❓로 잡되 어느 쪽을 구현이 택했는지 적는다. 산출물 backend/docs/audit/audit-H-명세매칭_노션용.md.
H 마지막 줄: audit-H: 영역 17 매칭율 N% · ✅a ⬆b 🔁c ⛔d ❓e · 재구현 후보 d건(소요 합계 h시간) · 결정 필요 e건

2부. 프론트 설계 대조: 정본 backend/docs/frontend/frontend-design.md §11의 지시문을 그대로 실행한다. 감사 H 결과를 먼저 읽고 STEP 4·5에 반영한다. 산출물 세 개는 backend/docs/frontend/ 에: frontend-design-actual.md · frontend-design-diff.md · api-types.d.ts.
마지막 줄: fe-design: 화면 N · API 매핑 ✅a 🔁b ❌c ⬆d · 백엔드 추가 필요 e건 · FE-1 착수 가능/불가
```

## 지시문 B (3단계 — A 마지막 줄이 온 뒤)

```
[개발용 기능 콘솔] 정본은 backend/docs/textbook/dev-console.md 다. 감사 G·감사 H·프론트 설계 대조가 전부 끝난 뒤에만 실행한다(하나라도 안 끝났으면 멈추고 보고). 먼저 정리.md·dev-console.md·backend/docs/audit/endpoints.md·frontend-handoff.md를 읽어라.

절대 규칙: 새 컨트롤러·서비스·매퍼·표·설정값 0개 / 파일은 src/main/resources/dev-console/ 5개(index.html·console.css·console.js·checks.js·README.md)만 / static·templates·Thymeleaf 금지 / 클래스는 com.templestamp.devconsole.DevConsoleConfig 하나(@Profile("!prod"), /console/** → classpath:/dev-console/, / → /console/ 리다이렉트) / SecurityConfig에 /console/** 허용 한 줄을 같은 !prod 조건 아래 주석 "DEV-CONSOLE: 프론트 결합 시 삭제"와 함께 / 토큰 localStorage 금지 / 콘솔이 만드는 데이터는 이메일 접두 console- 만, cleanup.sql에 삭제 1줄 / JUnit·newman 어디서도 콘솔을 참조하지 않는다 / Tailwind CDN 한 줄, 빌드 도구 없음.

STEP 1: DevConsoleConfig + 보안 한 줄 + 빈 index.html → dev 기동 → localhost:8080/ 이 /console/ 로 리다이렉트되고 200인지, prod 프로파일에서는 404·401인지(DC1·DC3) 먼저 확인하고 한 줄 보고.
STEP 2: 공통 뼈대 — 왼쪽 메뉴 10개(상태·회원·권역코스사찰·순례·현장인증·기록·완주인증서보상·원고·전자책인쇄·관리자) + 기능 체크 탭, 오른쪽 작업창, 아래 응답창(상태코드 배지 + JSON pretty + 소요 ms, 4xx 주황·5xx 빨강). fetch 래퍼 하나(Authorization 헤더·refresh 쿠키·401이면 refresh 1회 재시도). 상단 고정 문구: "개발용 콘솔 — 정식 판정은 newman·JUnit, 프론트 결합 시 삭제".
STEP 3: 메뉴 10개를 정본 §2 표대로. 각 작업창은 폼 → API → 응답창. 회원 탭에 테스트 계정 U1·ED1·ADM 원클릭 로그인(비번은 console.js 상수, dev 전용 명시). 현장 인증 탭은 좌표를 보내지 않는다는 문구, QR은 관리자 발급 토큰 붙여넣기. 기록 탭은 presign → PUT → 등록까지. 완주 탭의 진위 확인은 토큰 없이 새 창. 전자책 탭은 5초 폴링. 관리자 탭 청소기 실행은 건수 표시.
STEP 4: checks.js — F01~F58 행 + [확인]·[전체 실행]·집계. 순서 의존(앞 단계 id를 뒤가 씀, 실패 시 뒤는 건너뜀), 파괴적 기능은 console- 계정 + 확인창, 시간 기반 3건(F14·F51·F55)은 청소기 실행 대체 판정 + "대체" 표시, F58은 downloadUrl fetch 후 %PDF 확인(불가 시 "브라우저에서 미확인"). 결과 JSON 다운로드 버튼.
STEP 5: backend/docs/verify/dev-console-remove-check.sh — 폴더·클래스·보안 줄 셋 중 하나라도 남아 있으면 exit 1. DC6·DC7 실측: 셋을 임시 삭제 → 컴파일·JUnit 전체·run-all.sh 1회 통과 확인 → 원복. 정리.md §9에 "프론트 결합 시 삭제 목록 3개 + 스크립트" 기록.
STEP 6: 검증 DC1~DC7 전부 — endpoint-scan 91 불변, run-all.sh 2회 동일(콘솔 켠 채), 기능 체크 전체 실행 ✅ ≥ 55 · ❌ 0 · 5xx 0, security-check에 S26(prod에서 /console/** 404) 추가. dev-console/README.md에 사용법 5줄.
STEP 7: backend/docs/audit/dev-console.md(DC 결과·화면 캡처 경로·삭제 절차) / 정리.md §1(콘솔 존재, 91 불변)·§9 갱신.

마지막 줄: dev-console: DC 7/7 · 기능체크 ✅a ❌b 대체c · 엔드포인트 91 · newman N/N×2 · 삭제검사 통과
```

## 4단계 이후의 지시문
4~14단계 지시문은 **그때 제가 결과를 보고 만듭니다**(챕터 10은 H 결과에, 배포는 운영 환경 정보에, FE-1은 설계 대조 결과에 달려 있어서 지금 미리 쓸 수 없습니다). 각 단계마다 정본 md 1개 + 지시문 1개가 나오고, 넣는 곳은 백엔드는 `backend/docs/textbook/`, 프론트는 `backend/docs/frontend/`입니다.

## 예성이 미리 준비하면 좋은 것 (6·7단계용)
- 운영 서버(어디에 올릴지: Render/클라우드 VM 등)·운영 MySQL·도메인·파일 저장소 결정
- 카카오 개발자 콘솔에서 **JavaScript 키**(프론트용, REST 키와 다름)
- 원고 140편 CSV 정리 시작(콘텐츠 트랙, 코드와 무관)
