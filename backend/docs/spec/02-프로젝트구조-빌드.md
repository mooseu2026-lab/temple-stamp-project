# temple-stamp-project — 기획 명세 ② 프로젝트 구조 · 백엔드 트리 · 빌드

> 노션 원문을 md로 옮긴 것. ● 만들어진 파일 / ○ 아직 만들어지지 않은 파일 — **명세 작성 시점** 기준이며 현재 구현 상태가 아니다.

```
temple-stamp-project
├── backend
│   └── src/main/java/com/templestamp
├── frontend
└── docs
```

## 백엔드 트리

```
com.templestamp
├── ● TempleStampApplication          @SpringBootApplication · @ConfigurationPropertiesScan · @MapperScan(annotationClass = Mapper.class)
├── global
│   ├── config
│   │   ├── ● SecurityConfig          STATELESS · CORS · 경로별 권한
│   │   ├── ○ JacksonConfig           Asia/Seoul offset · fail-on-unknown-properties
│   │   ├── ○ WebConfig
│   │   └── ● AppProperties           jwt.secret / qr.secret 분리 보관
│   ├── security                      ★ JWT 핵심
│   │   ├── ● JwtProvider             HMAC-SHA 서명 · 발급 · 파싱
│   │   ├── ● JwtAuthenticationFilter Bearer 파싱 → SecurityContext
│   │   ├── ● AuthenticatedUser       record(userId, email, role)
│   │   ├── ● JsonAuthenticationEntryPoint   401 → ApiResponse JSON
│   │   └── ● JsonAccessDeniedHandler        403 → ApiResponse JSON
│   ├── error
│   │   ├── ● ErrorCode · ● BusinessException · ● ErrorResponse · ● GlobalExceptionHandler
│   ├── response
│   │   ├── ● ApiResponse<T> · ● PageResponse<T> · ● ItemsResponse<T>
│   ├── type (● 6개)
│   │   ├── Tier(AGE20~AGE60·RIDER·FOREIGN) · AccuracyGrade(HIGH·MID·LOW) · StampStatus(GPS_DONE·QR_DONE·COMPLETED·EXPIRED·PENDING·REJECTED)
│   │   └── VerifyMethod(GPS_QR·EVIDENCE) · UploadPurpose(PHOTO·EVIDENCE) · AgreementType(LOCATION_SERVICE·EBOOK_PUBLIC)
│   └── web
│       ├── ○ RequestIdFilter · ○ CoordinateFieldGuard(latitude/longitude/lat/lng → 400 COMMON-4001) · ○ CachedBodyRequest
├── auth      ● AuthController · AuthService · RefreshToken(해시 저장) · RefreshTokenMapper · dto(SignupRequest·LoginRequest·LoginResponse)
├── user      ○ UserController·UserService · ● User(12필드) · ○ UserAgreement · ● UserMapper · ○ UserAgreementMapper · dto ● 4
├── course    ○ Region/Course/CourseSite Controller·Service·Mapper · dto ● 8 (RegionRow/Response · CourseRow/Response · CourseSiteRow/Response · CourseDetailResponse · ProgressResponse)
├── site      ○ SiteController·SiteService·SitePageService · Site/SiteI18n/SiteViewpoint/SiteBadge/SiteDistance · Mapper 5 · dto ● SiteResponse · SitePageResponse(8블록 + guideAvailable)
├── verse     ○ VerseController·PhraseService · GwanVerse/ExpansionPhrase/Mission/PhraseSeen/TaskSeen · Mapper 5 · dto ● VerseResponse
├── pilgrimage ○ PilgrimageController·PilgrimageService·PassportService · Pilgrimage·Mapper · dto ● 4 (StartRequest · PilgrimageResponse · PassportSlotRow · PassportResponse)
├── stamp     ○ StampController·StampService·StampExpireService·CompletionService · ○ QrTokenProvider(qr.secret 분리) · Stamp·Mapper · dto ● 9
├── upload    ○ UploadController·UploadService·ObjectStorageClient · dto ● PresignResponse
├── photo     ○ PhotoService·Photo·PhotoMapper (dto 없음 — MissionSubmitRequest.photoKey로만)
├── thinkbox  ○ Controller·Service·Thinkbox·Mapper · dto ● 4
├── meditation ○ Controller·Service · Meditation/MeditationI18n/MeditationLog · Mapper 3 · dto ● 5
├── reward    ○ RewardController·RewardService·AuditScorer · RewardPolicy/UserReward · Mapper 2 · dto ● 3
├── certificate ○ Controller·Service·SerialGenerator · Certificate·Mapper · dto ● 3
├── ebook     ○ EbookController · PrintOrderController(/api/print-orders 3개) · EbookService · PrintOrderService(회향본 자격 검증 4단계) · Ebook/PrintOrder · Mapper 2 · dto ● 6
├── content   ○ ContentController·StaticContentLoader
├── kakao     ○ KakaoMapConfig·KakaoMapService · dto ● 4
└── admin     ○ AdminSite/Course/Stamp/Content/Reward/Ebook Controller · dto ● 12
```

## 리소스 구조

```
src/main/resources
├── application.yml · application-local.yml · application-prod.yml
├── db
│   ├── schema.sql          28개 테이블
│   ├── data.sql            마스터 데이터 (확장문구 175편 등)
│   └── data-local.sql      로컬 더미
├── mapper                  MyBatis XML — 도메인별 하위 폴더
└── static-content
    ├── guide.ko.json       사찰 가는 법
    └── i18n/ko.json · en.json
```

## build.gradle (명세 시점)

```groovy
plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.16'
    id 'io.spring.dependency-management' version '1.1.7'
}
group = 'com.templestamp'
version = '0.0.1-SNAPSHOT'
java { toolchain { languageVersion = JavaLanguageVersion.of(21) } }
configurations { compileOnly { extendsFrom annotationProcessor } }
repositories { mavenCentral() }
ext { mybatisVersion = '3.0.5'; jjwtVersion = '0.13.0' }   // Boot 3.x 계열. 4.x는 spring-boot 4가 딸려와 빈 중복
dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    implementation "org.mybatis.spring.boot:mybatis-spring-boot-starter:${mybatisVersion}"
    implementation "io.jsonwebtoken:jjwt-api:${jjwtVersion}"
    runtimeOnly "io.jsonwebtoken:jjwt-impl:${jjwtVersion}"
    runtimeOnly "io.jsonwebtoken:jjwt-jackson:${jjwtVersion}"
    runtimeOnly 'com.mysql:mysql-connector-j'
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
    annotationProcessor 'org.springframework.boot:spring-boot-configuration-processor'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testImplementation 'org.springframework.security:spring-security-test'
    testImplementation "org.mybatis.spring.boot:mybatis-spring-boot-starter-test:${mybatisVersion}"
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}
tasks.named('test') { useJUnitPlatform() }
```

JJWT는 `jjwt-api`·`jjwt-impl`·`jjwt-jackson` 분리 추가 방식. ZXing 3.5.4는 QR·바코드 생성용 최신 Java 릴리스.
