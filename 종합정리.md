# temple-stamp 백엔드 — 종합 정리 (2026-09-07 · G 보강 · 감사 H · 프론트 대조 반영)

이 문서 하나로 **지금 어디까지 왔고, 무엇이 남았고, 내가 무엇을 건의하는지**를 다 읽을 수 있게 썼다.
숫자는 전부 이 날 실제로 돌려 나온 값이다. 근거 파일은 항목마다 적어 두었다.

---

## 0. 한 줄

챕터 0~9 코드가 전부 닫혔고, 최종 점검 F 가 배포를 세 번 막았다가 챕터 9 보강으로 셋 다 풀렸다.
그 뒤 감사 G 로 남은 것을 전수로 훑었다.

**배포 진입 조건 12개 전부 ✅ · 배포 전 필수 3건 전부 닫힘 · 명세 매칭율 87%**

감사 G 가 모은 배포 전 필수 셋을 고쳤고(폰트 라이선스 · `refresh_token` 색인 · 시더 실행법),
기획 명세와 구현을 영역별로 맞춰 봤고(감사 H), 프론트 설계도를 실제 코드 기준으로 다시 그렸다.

```
API        91개 — ✅ 91 · ⚠ 0 · ❌ 0
기능        58개 — ✅ 58 · ⚠ 0 · ❌ 0
JUnit      266 / 266 (실제 MySQL + 로컬 저장소)
newman     515요청 / 2,388단언 실행 · 실패 0 · 2회 완전 일치(채점 파일 바이트 동일)
보안       S1~S25 · 세부 85항 전부 통과
DB         H1~H18 어긋남 0 (H17 새 방식 · H12 3/3 실측)
빈 DB      ALTER 사슬 ↔ schema.sql 전수 비교 → 차이 0
prod       드라이런 10항목 25/0 · 설정 착오 차단 3갈래 12/0 · ERROR 0
복구       덤프 0.17MB·1초 / 복원 1.4초 → 복원본 실행 결과 원본과 동일
부하       동시 20 · 3분 = 66,584요청 · 평균 54ms · 5xx 0 · 교착 0
첫 실행    저장소 복사본으로 처음부터 → 3분 41초에 초록
```

---

## 1. 이번에 한 일 — 일곱 덩이

| # | 무엇 | 결과 | 보고서 |
|---|---|---|---|
| 1 | 최종 점검 F (배포 전 전수 점검) | 배포 진입 12개 중 **9 ✅ · 3 ❌** | `audit/verify-ch0-9-결과_노션용.md` |
| 2 | 챕터 9 보강 — F 가 막은 셋을 고침 | 교착 0 · 빈 DB 차이 0 · 설정 착오 기동 차단 | `audit/ch9-hardening.md` |
| 3 | F 재판정 — ❌ 였던 셋만 다시 측정 | **12 ✅ · 0 ❌** | 같은 결과 문서(재판정 반영) |
| 4 | 감사 G — 잔여 작업 전수 12단계 | **잔여 38건** · 정본 정정 4 · 계약 어긋남 8 | `audit/audit-G-잔여작업_노션용.md` |
| 5 | G 보강 — 배포 전 필수 3 + DELETE 204 통일 | 필수 3/3 · `all.sh` 초록 · 첫실행 7분 19초 | `audit/g-hardening.md` |
| 6 | 감사 H — 기획 명세 ↔ 구현 매칭 | **매칭율 87%** · ⛔ 13(62시간) · ❓ 9 | `audit/audit-H-명세매칭_노션용.md` |
| 7 | 프론트 설계 대조 | 화면 21 · ❌ 4 · 백엔드 추가 5 · **FE-1 착수 가능** | `frontend/frontend-design-diff.md` |

---

## 2. 최종 점검 F 가 찾아낸 것 — 넷

전부 **기동도 성공하고 로그도 조용한** 종류였다. 기능 58개가 모두 ✅ 인데 배포가 막힌 이유가 이것이다.
F 표는 "그 기능이 규칙대로 도는가" 를 보고, 아래 넷은 "요청이 겹칠 때 · 운영 DB 로 올릴 때" 만 드러난다.

### ① 청소기와 미션 제출이 교착한다 — 사용자에게 500 이 나갔다

청소기의 세션 만료는 조건을 훑으며 한 번에 지우는 UPDATE 였다. 그러면 **보조 색인을 먼저, PRIMARY 를 나중에** 잠근다.
미션 제출은 `WHERE stamp_id = ?` 라 **PRIMARY 를 먼저** 잠근다. 순서가 엇갈려 교착이 나고, 진 쪽이 사용자 요청이면
그대로 500 이 나갔다. 만료 대상 3,000건을 두고 동시 20 으로 60초 돌려 **교착 8건 · 사용자 500 5건**을 실측했다.

평소 검증에 한 번도 안 걸린 이유가 중요하다. **만료 대상이 없으면 그 UPDATE 는 아무 행도 잠그지 않는다.**
검증 세트는 앞뒤로 정리를 하니 대상이 늘 0이었다. 운영은 반대다 — 5분마다 도는데 버려진 세션은 늘 쌓인다.

**고친 방식**: 고르기(잠그지 않고 id 만 PK 순으로)와 갱신(그 id 만 PK 로)을 나누고 200건씩 끊어 커밋한다.
같은 조건에서 다시 재니 **교착 0 · 500 0**. 재현 시험은 `verify/load/deadlock-repro.sh` 로 상시화했다.

### ② ALTER 파일에 색인 하나와 **표 하나**가 빠져 있었다

`schema.sql` 로 새로 세운 DB 와 ALTER 로 올린 DB 가 갈라져 있었다.

| 무엇 | 증상 |
|---|---|
| `idx_storage_orphan_pending` | `schema.sql` 은 `(status, enqueued_at)`, ALTER 로 올린 DB 는 `(deleted_at, enqueued_at)`. 청소기가 `status='PENDING'` 으로 큐를 뒤질 때 색인이 하는 일이 없다 |
| **`cert_serial` 표** | `schema.sql` 에만 있고 **어떤 ALTER 파일도 만들지 않는다.** ALTER 로 올린 운영 DB 에는 표가 아예 생기지 않고, **첫 완주가 나오는 날 인증서 발행에서 터진다** |

색인은 F 가, 표는 그 뒤에 만든 상시 검사(db-check H17)가 찾았다. 둘 다 넣었고 이제 매 검증에서 자동으로 대조한다.
`정리.md` §5-1 에는 "`cert_serial` 은 신규 표라 알아서 만들어진다" 고 **적혀 있었다** — 문서가 사실을 앞질러 적힌 자리였다.

### ③ 설정이 거짓말을 하고 있었다

`application-prod.yml` 에 `storage.provider: s3` 라고 적혀 있는데 **`provider` 를 읽는 코드가 한 곳도 없다.**
운영에 올려도 파일은 서버 로컬 디렉터리에 쌓인다. 설정만 보고 "S3 로 간다" 고 읽으면 배포한 뒤에야 안다.

구현이 없는 값을 조용히 무시하느니 **그 값으로는 뜨지 않게** 막았다. 같은 원칙으로 하나 더 막았다 —
prod 에서 `PUBLIC_BASE_URL` 에 localhost 가 들어 있으면 기동이 실패한다. 그 값은 **인증서 QR 에 그대로 찍히고,
종이로 나간 뒤에는 되돌릴 수 없다.**

### ④ 기동 직후에 트래픽을 붙이면 500 이 난다

기동 13초 뒤에 동시 20 으로 때렸더니 500 이 21건 났다. 커넥션 풀이 아직 한 개일 때 몰려 획득 제한(30초)을 넘긴 것이다.
예열된 뒤 같은 부하에서는 7만 요청에 5xx 가 0 이다. **운영은 획득 제한이 3초라 더 빨리 500 이 된다.**
코드가 아니라 배포 절차 문제라 `정리.md` §5-6 에 예열 절차로 넣었다.

---

## 3. 감사 G 가 찾아낸 것 — 배포 전 필수 셋 (전부 §6-1 에서 닫혔다)

| # | 무엇 | 왜 지금인가 | 예상 |
|---|---|---|---|
| 1 | **폰트 OFL 라이선스 원문이 없다** | `NotoSansKR-Regular.ttf` 6.1 MB 가 jar 로 배포된다. OFL 1.1 은 사본 동봉을 요구한다. 폰트를 빼는 선택지는 없다 — 빼면 PDF 한글이 □ 로 나간다 | 10분 |
| 2 | **`refresh_token.expires_at` 에 색인이 없다** | 청소기가 5분마다 표 전체를 훑고 정렬한다(EXPLAIN `type=ALL` + `filesort`). 로그인 한 번에 한 행이 쌓이고 수명이 7일이라 운영에서는 수십만 행이 된다 | 20분 |
| 3 | **시더 실행법이 어느 문서에도 없다** | `정리.md` §2 대로만 하면 사찰 5곳·코스 1개만 들어가고 `run-all.sh` 가 단언 30건 실패로 끝난다. 실제로 복사본에서 재현했다 | 1시간 |

셋째가 특히 아프다. **처음 보는 사람이 이 저장소로 초록을 볼 수 있는가** 를 재려고 저장소를 통째 복사해
`.env.local` 까지 지우고 돌려 봤더니, 막히는 지점이 정확히 그 한 줄이었다.
`SPRING_PROFILES_ACTIVE=seed ./gradlew bootRun` 을 §2 에 넣으면 **3분 41초**에 초록을 본다(실측).

---

## 4. 검증 도구에서 잡은 결함 — 여덟

이 프로젝트에서 반복되는 고장 방식은 하나다. **실패로 드러나지 않고 분모나 기준이 조용히 어긋난다.**

| # | 도구 | 무엇이 틀렸나 |
|---|---|---|
| 1 | db-check·security-check·cleanup·checkpoints·run-all | 스키마 이름이 박혀 있어 빈 DB 리허설에서 **개발 DB 를 채점**했다 |
| 2 | 테스트 2개 + 회향 SQL 2개 | 코스를 번호 범위(`BETWEEN 37 AND 48`)로 골랐다 — 개발 DB 의 auto_increment 가 그랬을 뿐 |
| 3 | all-checkpoints.sql | 5분 스케줄러와 경합하는 수를 세어 **실행마다 값이 달랐다**(2 vs 1) |
| 4 | db-check H18 | 큐 전체를 세어 다른 검사가 남긴 행에 판정이 흔들렸다 |
| 5 | StorageRealityTest | 배치 전체의 실패 수를 단언해 다른 테스트의 잔여에 흔들렸다 |
| 6 | prod 드라이런 스크립트 | 작업 디렉터리를 잘못 짚었고, 청소기 첫 줄이 나오는 1분을 안 기다리고 판정했다 |
| 7 | H17 스키마 비교 | 두 스키마를 **다른 문자셋으로** 올려 같은 제약이 26건 다르다고 나왔다 |
| 8 | 교착 재현 시험 | 만료 대상이 없으면 "교착 0" 이 나온다 — 통과가 아니라 **재지 못한 것** |

감사 G 에서 하나 더 겪었다. 느린 질의 로그를 켜고 newman 을 돌렸는데 앱 질의가 **한 건도** 안 잡혔다.
`long_query_time` 은 접속할 때의 값을 물고 있는 세션 변수라, 이미 열린 커넥션 풀은 옛 값을 쓴다.
**켠 다음에 앱을 기동**해야 잡힌다. 도구가 조용히 아무것도 재지 않은 자리였다.

---

## 5. 지금 상태 — 배포 진입 판정 12/12

| # | 조건 | 값 | 판정 |
|---|---|---|---|
| 1 | 기능 ⚠ 0 · ❌ 0 | ✅58 | ✅ |
| 2 | 엔드포인트 ⚠ 0 | 91 전부 ✅ | ✅ |
| 3 | 2회 동일 | 채점 파일 바이트 동일 | ✅ |
| 4 | 5xx 0 | newman 0 · 부하 0 · 교착 재현 0 | ✅ |
| 5 | 보안 S1~S25 | 세부 85 전부 통과 | ✅ |
| 6 | DB H1~H18 | 어긋남 0 · H12 3/3 실측 | ✅ |
| 7 | 빈 DB 차이 0 | 0 (일곱 항목 전부) | ✅ |
| 8 | prod 드라이런 ERROR 0 | 0 | ✅ |
| 9 | 복구 리허설 동일 | 요청·단언·채점 모두 같음 | ✅ |
| 10 | 부하 교착 0 | 정식 3분 0 · 재현 0 | ✅ |
| 11 | 도구 4/4 | 자가검증 4/4 + 개수 단언 91 | ✅ |
| 12 | 인수인계 2종 | `인수인계_temple-stamp_최종.md` · `frontend-handoff.md` | ✅ |

---

## 6. 배포할 때 사람이 확인할 열두 줄

빠뜨려도 **기동은 성공하고 에러도 안 나는** 것들이다. 전문은 `정리.md` §5.

| # | 항목 | 빠뜨리면 |
|---|---|---|
| 1 | 운영 DB 에 ALTER 네 파일(ch7 → ch1-withdraw → ch8 → ch9) | 색인이 옛것이거나 `cert_serial` 이 없어 첫 인증서에서 터진다 |
| 2 | 운영용 JWT·QR 시크릿 별도 발급(서로 다르게) | 로컬 값이 운영에 남는다 |
| 3 | actuator 노출이 `health` 하나인지 — **파일 두 곳** | `/env` 가 루트에 열리고 거기 시크릿이 있다 |
| 4 | `app.cookie.secure: true` | Secure 없는 쿠키가 나간다 |
| 5 | `cors.allowed-origins` 를 배포 도메인으로 | 로컬 기본값이 남는다 |
| 6 | `PUBLIC_BASE_URL` 운영 도메인 | 이제는 기동이 막힌다 |
| 7 | `STORAGE_BUCKET`·`STORAGE_ENDPOINT` | prod 기동이 막힌다 — 막히는 것이 맞다 |
| 8 | MySQL 서버 `time_zone = '+09:00'` 명시 | 하루 한도·회상 날짜가 밀린다 |
| 9 | 한글 폰트가 jar 에 들어갔는지 | **PDF 한글이 □ 로 나간다 — 예외도 경고도 없이** |
| 10 | 청소기는 단일 인스턴스 전제 | 여러 대로 늘리려면 분산 잠금이 먼저다 |
| 11 | 배포 직후 예열 요청 뒤 트래픽 개방 | 커넥션 획득 3초를 넘겨 500 이 난다 |
| 12 | 파일 저장소 백업 — DB 덤프와 같은 시각 일 1회 · 30일 보관 | DB 만 되돌리면 실물이 없다 |

여기에 감사 G 의 배포 전 필수 셋(§3)을 더하면 배포 전 할 일이 전부다.

---

## 6-1. G 보강으로 닫힌 셋

| 무엇 | 무엇을 했나 | 실측 |
|---|---|---|
| 폰트 OFL 라이선스 | `fonts/OFL.txt` 동봉 | jar 를 풀어 `BOOT-INF/classes/fonts/OFL.txt` 확인 |
| `refresh_token` 색인 | `schema.sql`·`alter-ch9.sql` ⑱·개발 DB | EXPLAIN **`ALL`+filesort·2,037행 → `range`·1행** |
| 시더 실행법 | `README.md` 신설 + `정리.md` §2-0 | 복사본으로 **7분 19초**에 초록 |

함께 한 것 둘 — **DELETE 다섯을 204 · 본문 없음으로 통일**(컨트롤러 3 · 컬렉션 3 · JUnit 2 · 문서 4),
그리고 **`backend/docs/verify/all.sh`** — newman 2회 → DB → 보안 → 교착 재현을 순서대로 돌리고 하나라도 깨지면 그 자리에서 멈춘다(6분 13초).
`정리.md` §5 의 첫 줄이 "`all.sh` 가 초록인지 본다" 로 바뀌었다.

이 과정에서 **도구가 나를 잡았다.** `endpoints.md` 표에 굵은 글씨로 204 를 적었더니 프로브가 다섯 줄을 못 읽어
91 을 86 으로 셌고 `all.sh` 가 보안 84/1 로 멈췄다. 그 파일은 스캐너가 생성하는 것이라 손으로 고칠 자리가 아니었다.

---

## 6-2. 감사 H — 기획 명세와 얼마나 맞나

**영역 17 매칭율 87% · ✅62 ⬆14 🔁12 ⛔13 ❓9.** 전문은 `audit/audit-H-명세매칭_노션용.md`.

가장 컸던 물음 — 확장문구·행동과제 체계를 챕터 8 원고가 **대체했는가** — 의 답은 **아니다. 둘이 병존한다.**
`markQrDone(stampId, mission.getMissionId(), page.getManuscriptId(), …)` 한 줄이 두 체계의 id 를 함께 박는다.
싱글페이지에는 명세 체계(`expansion_phrase`·`mission`, tier 축)가 나가고, 도장에 남는 글은 원고(`manuscript`, 사찰 축)다.
**프론트는 이 둘이 다를 수 있다는 것을 알아야 한다.**

되돌릴 값이 분명한 넷(합쳐 15시간)만 추리면 이렇다.

| 무엇 | 명세 | 지금 | 소요 | 왜 되돌릴 만한가 |
|---|---|---|---|---|
| refresh 수명 | 14일 | 7일 | 1h | 설정값 하나. 재로그인 주기는 사용자가 체감한다 |
| 사진 `has_other_face` 전자책 제외 | 제외 | 안 본다 | 2h | **개인정보다.** 조회 조건 한 줄 |
| 확장문구 권역 내 중복 금지 | 금지 | 없음 | 4h | 한 코스에서 같은 문구를 두 번 보면 "다섯 곳" 느낌이 깨진다 |
| 행동과제 50/30/20 가중 | 사찰50·구절30·공통20 | 가중 없음 | 8h | "사찰마다 다른 과제" 가 이 앱의 핵심인데 지금은 구절만 본다 |

나머지 아홉은 유지 쪽을 권한다 — EPUB(24h) · INTERIM 중간본 · 인쇄 자격 · 명상 등록 API · 다국어 · 상태코드 셋 · EXIF.
상태코드 셋(410/422 ↔ 409/400)은 **명세를 고치는 편이 싸다** — 나간 코드는 클라이언트가 분기하는 키다.

---

## 6-3. 프론트 설계 대조 — FE-1 착수 가능

`api-types.d.ts` 를 **코드에서 자동으로 뽑았다**(엔드포인트 91 · DTO 104 · enum 7 · 타입 미정 0).
생성기는 `frontend/generate-api-types.js` 이고 스스로 검사한다 — 중괄호 균형·이상한 줄·미정 타입이 있으면 종료코드 1.

| 갈래 | 수 | 무엇 |
|---|---:|---|
| 화면 일치 | 16 | |
| 차이(🔁) | 4 | 홈(전용 API 없음·넷을 병렬로) · 생각상자·설정(DELETE 204) · 전자책(`cancelable` 없음) |
| 정본이 가정했는데 없는 것(❌) | 4 | 코스 상세의 원고 미리보기 · 전자책 `cancelable` · 홈 묶음 API · 원고 다국어 |
| 백엔드 추가 필요 | 5 | 어느 것도 **프론트 착수를 막지 않는다** |

정본이 틀린 곳 하나 — 로그아웃은 204 가 아니라 **200 · `Set-Cookie: Max-Age=0`** 이다(POST 라 204 통일 대상이 아니었다).
그리고 정본은 "FE-1 완료 조건 = 개발용 콘솔 삭제" 라고 적었는데 **콘솔이 아직 없다** — 만들지 않는다면 그 조건을 빼야 한다.

---

## 7. 남은 일 38건 — 갈래별 요약

전문은 `audit/audit-G-잔여작업_노션용.md` 에 번호·출처·권고까지 있다. 여기는 갈래만 적는다.

| 갈래 | 수 | 대표적인 것 |
|---|---:|---|
| 배포 전 필수 | **0** | 셋 다 닫혔다(§6-1) |
| 배포 후 | 12 | 원고 140편 반입 · QR 위치 힌트 답사 · 커넥션 풀 크기 · S3 · ShedLock · 결제 · 중간본 · 상시 검사 CI · postman README |
| 보류 | 8 | `site.name` 색인 · 컬럼 코멘트 · 폰트 서브셋 · 옛 Postman 자산 · 다행 허용 여부 |
| 결정필요 | 9 | DELETE 상태코드 통일 · 이름 쌍 · 교재 표기 · **명세와 다른 HTTP 상태 4건**(410/422 ↔ 409/400) · 손확인 문서 위치 |
| 정본 정정 | 4 | `CERT-4040`→4041 · 보상 상태 이름 셋 · `MS-4001` · `COURSE-4042` |
| 문서 닫기 | 2 | `ch4-close.md` Q-b · `ch7-completion.md` [질문] 중 둘 |

**배포 전 필수 3건 외에는 코드가 막고 있는 것이 없다.** 나머지는 콘텐츠·운영·프론트·결정의 몫이다.

### 7-1. 감사 도중에 기획 명세가 들어왔다 — 감사 H 가 남았다

작업하는 사이에 `backend/docs/spec/` 다섯 개(기획개요·기능·API·규칙 / 프로젝트 구조 / DB 28표 / DTO 79 / 프론트 설계)와
교재 `spec-match-audit.md`(**감사 H** 지시문)가 저장소에 추가됐다. 감사 H 는 명세와 구현을 영역별로 맞춰
**매칭율을 숫자로 재고 100% 미만인 것을 "재구현 / 의도적 변경 / 미결" 로 가르는** 일이다.

감사 G 는 그 명세를 정본으로 삼아 **에러코드까지만** 다시 쟀고, 거기서 네 건이 나왔다.

| 무엇 | 명세 | 구현 |
|---|---|---|
| 세션 60분 만료 | `STAMP-4101` · **410** | `STAMP-4091` · **409** |
| QR 무효(다른 사찰·구버전) | `STAMP-4221` · **422** | `STAMP-4004` · **400** |
| 다짐 문장 길이 | 최소 **3자** · `STAMP-4222` · 422 | 최소 **10자** · `COMMON-4000` · 400 |
| 사진 EXIF 포함 | `UPLOAD-4221` · 422 | **없다** — 앱이 저장소에 직접 올려 서버가 확인할 방법이 없다(코드 주석에 사유) |

이 넷은 "틀렸다" 가 아니라 **"명세와 다르다"** 다. 의도적 변경인지 미달인지 가르는 것이 감사 H 의 몫이라
여기서는 목록만 남겼다. 그 밖에 끝자리 규칙 차이 8건(명세 `4041` ↔ 구현 `4040`)과 접두사 차이 3건이 있는데,
뜻이 같고 번호만 한 칸 달라 **하나의 규칙 차이**로 본다.

---

## 8. 건의 — 내가 하고 싶은 말

**① 이 프로젝트의 결함은 늘 "조용한" 쪽이었다.** 폰트를 빼면 한글이 □ 로 나가고, 색인을 빼면 큐가 느려지고,
`cert_serial` 을 빼면 완주가 처음 나오는 날 터진다. 셋 다 기동은 성공하고 로그는 깨끗하다.
그래서 **"돌았는가" 가 아니라 "몇 건을 처리했는가", "키가 있는가" 가 아니라 "바이트가 읽히는가"** 로 재는 검사를
늘려 온 것이 맞았다고 본다. H18·저장소 실재 검사·H17·교착 재현 넷이 그렇게 만들어졌고, **넷 다 실제로 결함을 찾았다.**

**② 도구를 먼저 의심하는 습관이 아홉 번 값을 했다.** "전부 통과" 가 나올 때마다 도구를 흔들어 봤고,
그때마다 도구가 틀린 자로 재고 있었다. 앞으로 검사를 추가할 때도 **일부러 깨뜨려 보고 잡히는지 확인**하고 넣기를 권한다.

**③ 상시 검사 넷을 CI 로 옮기면 좋겠다.** `run-all.sh` · `db-check.sh`(H17·H18 포함) ·
`security-check.sh` · `deadlock-repro.sh` 넷이다. 지금은 사람이 기억해서 돌려야 하는데,
이번에 나온 결함 중 셋은 **한 번도 안 돌린 조건**에서 나왔다.

**④ 배포 직후 예열은 절차로 강제하는 편이 낫다.** 로컬에서 30초 걸린 것이 운영에서는 3초 만에 500 이 된다.
헬스체크만으로는 풀이 안 찬다 — 실제 조회를 몇 번 태우고 트래픽을 붙이는 것을 배포 스크립트에 넣기를 권한다.

**⑤ 문서가 코드보다 먼저 낡는다.** `cert_serial` 이 그랬고, 시더 실행법이 그랬다.
전자는 "그렇게 되어 있다" 고 적혔지만 아니었고, 후자는 아예 적히지 않았다.
**문서에 "그렇게 되어 있다" 고 적을 때는 그것을 확인하는 검사를 같이 만드는 것**이 이 프로젝트에 맞는 방식 같다.
H17 이 그 예다 — 문장 대신 검사를 넣었더니 문장이 틀렸다는 것을 스스로 알려 줬다.

**⑥ 처음 보는 사람 기준의 리허설을 가끔 돌리기를 권한다.** 복사본으로 4분이면 끝난다.
개발 DB 에서만 통과하는 것, 내 장비에만 있는 것, 문서에 안 적힌 것이 그 4분에 전부 드러난다.

---

## 9. 질문 — 답을 주면 바로 반영한다

| # | 질문 | 지금 상태 |
|---|---|---|
| 1 | **감사 H 의 ⛔ 13건을 어떻게 할까** | 되돌릴 값이 분명한 넷(15시간)을 §6-2 에 추렸다. 나머지 아홉은 유지 + 명세 정정을 권한다 |
| 2 | DELETE 세 개의 상태코드를 204 로 맞출까 | 프론트 계약이 바뀐다. 붙기 전이면 지금이 싸다 |
| 3 | 교재(정본)의 에러코드 표기 4+8건을 내가 고쳐도 되는가 | 정본은 손대지 않는 것이 규칙이라 §3 표로만 만들어 뒀다 |
| 4 | 상시 검사 넷을 CI 로 옮길까 | 지금은 사람이 기억해서 돌린다 |
| 5 | 커넥션 풀 크기를 지금 정할까, 배포 후 트래픽을 보고 정할까 | 운영 20 으로 지금 부하에서 5xx 0 |
| 6 | 문서 정리 범위 — §10 | 지금은 **아무것도 지우지 않았다** |
| 7 | **감사 H(명세 ↔ 구현 매칭)를 이어서 할까** | 지시문(`textbook/spec-match-audit.md`)과 명세 5종이 들어와 있다. G 는 에러코드까지만 봤다 |

---

## 10. 문서 정리 — 확인하고 결정할 것

"필요없는 md 정리" 를 하려고 저장소의 마크다운을 전부 대조했다. 결론부터 적는다.

**지금 지워도 되는 것은 없다.** 56개가 이렇게 나뉜다.

| 갈래 | 수 | 왜 남겨야 하나 |
|---|---:|---|
| 교재(`backend/docs/textbook/`) | 21 | 네가 준 정본이다. 손대지 않는다 |
| **기획 명세(`backend/docs/spec/`)** | 5 | 작업 중에 새로 들어왔다. 감사 H 의 정본 |
| `정리.md` | 1 | 매 세션의 기준 문서 |
| 챕터·점검 보고서(`backend/docs/audit/`) | 24 | **전부 `정리.md` 나 교재가 이름으로 가리키고 있다.** 지우면 그 링크가 깨진다 |
| 루트 요약(`결과.md`·`제시.md`) | 2 | 교재 넷과 `정리.md` 가 가리킨다 |
| 이번에 만든 것 | 3 | `종합정리.md`(이 문서) · `꼭남겨야할내용.md` · `postman/예성-손확인.md` |

참조를 하나씩 세어 봤다 — `feature-status-ch0-5`(교재 2곳+정리.md) · `verify-ch0-7`(3곳) ·
`ch7-unused`(3곳) · `seed-v4`(6곳). 오래돼 보이는 것도 링크가 살아 있다.

**그래서 이번에는 아무것도 지우지 않았다.** 지우려면 참조하는 문서를 함께 고쳐야 하는데
그중 다수가 손대지 않기로 한 교재다. 정리를 원하면 둘 중 하나를 골라 주면 된다.

| 안 | 무엇 | 잃는 것 |
|---|---|---|
| A | `backend/docs/archive/` 를 만들어 **점검 B·C·D·E 결과 4개와 챕터별 미사용 코드 보고서 2개**를 옮긴다(지우지 않음) | 교재의 링크가 경로만 어긋난다. 내용은 남는다 |
| B | 그대로 둔다 | 파일 수가 56개로 유지된다 |

지운 것은 하나뿐이다 — 루트에 셸 실수로 생긴 **0바이트 파일 `=`**.

`꼭남겨야할내용.md` 는 다른 문서가 다 사라져도 **그것만 있으면 다시 세울 수 있는 것**만 골라 담았다.

---

## 11. 근거 파일

| 무엇 | 어디 |
|---|---|
| 최종 점검 F 결과(재판정 반영) | `backend/docs/audit/verify-ch0-9-결과_노션용.md` |
| F 실행 원문 25개 | `backend/docs/audit/verify-f-logs/` |
| 챕터 9 보강 보고서 | `backend/docs/audit/ch9-hardening.md` |
| 보강 실행 원문 8개 | `backend/docs/audit/ch9-hardening-logs/` |
| **감사 G 잔여 작업 38건** | `backend/docs/audit/audit-G-잔여작업_노션용.md` |
| 교체 전 파일 | `backend/docs/audit/ch9-hardening-replaced/` · `verify-f-toolcheck/` |
| 인수인계 | `backend/docs/audit/인수인계_temple-stamp_최종.md` |
| 프론트 전달 | `backend/docs/audit/frontend-handoff.md` |
| 손으로 확인하는 10단계 | `postman/예성-손확인.md` |
| **G 보강 보고서** | `backend/docs/audit/g-hardening.md` |
| **감사 H — 명세 매칭** | `backend/docs/audit/audit-H-명세매칭_노션용.md` |
| **프론트 설계도(실제 기준)** | `backend/docs/frontend/frontend-design-actual.md` |
| **프론트 대조표** | `backend/docs/frontend/frontend-design-diff.md` |
| **API 타입 초안(자동 생성)** | `backend/docs/frontend/api-types.d.ts` · 생성기 `generate-api-types.js` |
| 검증 한 벌 | `backend/docs/verify/all.sh` |
| 처음 띄우기 | `README.md` |
| 기준 문서 | `backend/docs/정리.md` |
| 상시 검사 | `postman/run-all.sh` · `backend/docs/verify/db-check.sh` · `security-check.sh` · `load/deadlock-repro.sh` |
