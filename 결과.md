# 검증 결과 — 챕터 10 명세 정합 (배포 진입 12/12 · 매칭율 92%)

2026-09-07 실측 · 상세 `backend/docs/audit/verify-ch0-9-결과_노션용.md` · 보강 `audit/ch9-hardening.md`
실행 원문 `audit/verify-f-logs/` · `audit/ch9-hardening-logs/`
직전 회차(챕터 9)는 이 문서 아래 「이전 회차」에 남겨 뒀다.

```
API      91개 — ✅ 91 · ⚠ 0 · ❌ 0            (비로그인 실측 91 · 어긋남 0)
기능     58개 — ✅ 58 · ⚠ 0 · ❌ 0
newman   552요청 / 2562단언 실행 · 실패 0 · 2회 완전 일치(SQL 채점 바이트 동일)
JUnit    279 / 279 — 챕터 10 이 13건 신설(가중 5 · 버전 4 · 사진 2 · 취소가능 2)
보안     S1~S25 · 세부 85항 전부 통과 · 실패 0
DB       H1~H18 — 전부 어긋남 0 (H12 는 정본 순서로 다시 재어 3/3 실측)
빈 DB    ALTER 사슬 ↔ schema.sql 전수 비교(상시 검사 H17) → 차이 0
prod     드라이런 10항목 25/0 · 설정 착오 차단 세 갈래 12/0 · ERROR 0
복구     덤프 0.17MB·1초 / 복원 1.4초 → 복원본 실행 결과 원본과 동일
부하     동시 20 · 3분 = 66,584요청 · 평균 54ms · 5xx 0 · 교착 0 (재현 시험도 0)
도구     자가검증 4/4 · 개수 단언 91 · 잡아 고친 도구 결함 8건
```

**배포 진입 조건 12개 전부 ✅ · 기획 명세 매칭율 87% → 92%.**

챕터 10 이 감사 H 의 ⛔ 다섯을 닫았다 — refresh 14일 · 행동과제 50/30/20 가중 ·
확장문구 권역 내 버전 중복 금지 · 전자책에서 타인 얼굴 사진 제외 · 인쇄 자격 결정 기록.
여기에 프론트가 요구한 `cancelable` 을 더했다. **엔드포인트는 91 그대로다.**

가장 큰 것은 행동과제다. 지금까지 축이 (구절 × 계층) 하나뿐이라 **어느 사찰에서 열어도 같은 과제**가 나왔다.
`scope` 를 더해 SITE / VERSE / COMMON 세 풀로 나누고 50/30/20 으로 가중했다 — 5,000회 표본으로 ±5%p 안에 든다.


첫 판정은 9 ✅ · 3 ❌ 였다. 막았던 것은 둘 — `alter-ch9.sql` 이 색인 하나를 다시 만들지 않아
**ALTER 로 올린 운영 DB 만 옛 색인**을 갖게 되는 것, 그리고 청소기의 세션 만료 일괄 UPDATE 가
미션 제출과 **잠금 순서를 거꾸로** 잡아 교착과 사용자 500 을 만드는 것.
챕터 9 보강에서 둘 다 고치고 그 셋만 다시 쟀다(`audit/ch9-hardening.md`).

고치는 과정에서 같은 뿌리의 결함 하나를 더 찾았다 — **`cert_serial` 표를 만드는 ALTER 가 어디에도 없었다.**
ALTER 로 올린 운영 DB 에는 표가 생기지 않고 첫 완주가 나오는 날 인증서 발행에서 터진다.
비교를 "개발 DB 대 새 DB" 에서 "ALTER 사슬 대 schema.sql" 로 바꾸자마자 나왔다.

| 조건 | 첫 판정 | 재판정 | 조건 | 첫 판정 | 재판정 |
|---|---|---|---|---|---|
| 기능 ⚠0 ❌0 | ✅ | ✅ | 빈 DB 차이 0 | ❌ (2) | **✅ (0)** |
| 엔드포인트 ⚠0 | ✅ | ✅ | prod 드라이런 ERROR 0 | ✅ | ✅ |
| 2회 동일 | ✅ | ✅ | 복구 리허설 동일 | ✅ | ✅ |
| 5xx 0 | ❌ (5건) | **✅ (0)** | 부하 교착 0 | ❌ (재현 8) | **✅ (0)** |
| 보안 S1~S25 | ✅ | ✅ | 도구 4/4 | ✅ | ✅ |
| DB H1~H18 | ✅ | ✅ | 인수인계 2종 | ✅ | ✅ |

---

# 이전 회차 — 챕터 9 (전자책 · 인증서 PDF · 인쇄주문 · 청소기)

2026-09-07 실측 · 상세 `backend/docs/audit/ch9-ebook.md` · `ch9-unused.md`

## 이 챕터가 한 일 — 글자를 파일로 바꿨다

지금까지 사용자의 기록은 API 응답으로만 존재했고 손에 쥘 것이 없었다.

| 무엇 | 어떻게 |
|---|---|
| 전자책 | 도장·사진·문장·그날의 확장문구·생각상자·명상·인증서를 묶은 PDF 한 권 |
| 인증서 PDF | 번호·이름·코스·날짜 + **진위 확인 QR**. 첫 조회 때 만든다 |
| 인쇄주문 | READY 전자책을 종이로. 상태 여섯(REQUESTED→CONFIRMED→PRINTING→SHIPPED→DONE/CANCELED) |
| 청소기 | 5분마다 하나 — 파일 삭제 큐·만료 토큰·전자책 조판·도장 세션 |
| 관리자 사용자 목록 | 기본 ACTIVE, `?status=DELETED` 를 명시할 때만 탈퇴 계정 |

축은 **스냅샷 해시** 하나다. 재료(도장 id·사진 키·원고 id·생각상자 id·인증서 번호)를 정렬해
SHA-256 을 낸 값이 같으면 같은 책이고, 그러면 새로 만들지 않고 있던 것을 준다.
버튼을 여러 번 눌러도 책이 쌓이지 않는다.

---

## 시작하자마자 드러난 것 — 저장소에는 파일이 없었다

`ObjectStorageClient` 는 **서명만 만들고 바이트를 저장하지 않았다.** presign 이 가리키는
`localhost:9000` 에는 아무 서버도 없다. 챕터 6 의 사진도 실제로는 키만 기록됐다.

PDF 를 만들려면 바이트를 둘 곳이 있어야 하고 청소기가 지울 것도 있어야 해서,
`provider=local` 일 때 디렉터리에 실제로 쓰는 네 메서드를 붙였다(의존성 추가 없음).
그 대가로 **E06(다운로드 URL 을 열어 `%PDF` 확인)은 newman 으로 못 잰다** — 로컬에 S3 대역이 없다.
서명 모양은 newman 이, 파일 내용(`%PDF`·한글)은 JUnit 이 저장소에서 직접 읽어 확인한다.

---

## 이번에 잡은 제품 결함 넷

| # | 무엇 | 어떻게 드러났나 |
|---|---|---|
| 1 | 저장소가 바이트를 저장하지 않았다 | PDF 를 만들려다 둘 곳이 없어서 |
| 2 | **탈퇴 연쇄가 FK 로 막혀 500** — 남겨 둔 인쇄 주문이 지우려는 전자책을 붙들고 있었다 | 폴더 E 의 E25 |
| 3 | 하루 상한이 완주 코스본까지 세어, **완주한 날 개인 소장본을 못 만든다** | 폴더 E 의 E16b 가 429 로 |
| 4 | 만료 토큰 정리가 **한 번도 지운 적이 없었다** — 주기(하루)가 토큰 수명보다 짧다 | 점검 E 의 1,674행 |

②의 정리한 규칙: 확인 전 주문은 취소한 뒤 **행까지 지우고**(그래야 그 책도 지운다),
CONFIRMED 이후는 취소하지 않고 `needs_review` 만 켠 채 **책을 남긴다**(주문서가 가리키는 것이
사라지면 안 된다). 순서는 주문이 먼저, 전자책이 나중이다.

④에서 얻은 규칙 하나 — **도는 것과 하는 것은 다르다.** 스케줄러는 "돌았는가" 가 아니라
"몇 건을 처리했는가" 를 로그에 남겨야 한다.

---

## 점검 도구·테스트도 여섯 곳 틀려 있었다

챕터 9 가 데이터를 늘리자 비로소 드러난 것이 넷이다.

| # | 무엇 | 고침 |
|---|---|---|
| 1 | `cleanup.sql` 이 계정 블록에 `photo` 를 빠뜨려, 사진을 올린 계정에서 FK 로 멈췄다 | 블록에 추가 |
| 2 | `cleanup.sql` 의 계정별 `ebook` 삭제가 다른 계정의 주문 때문에 막혔다 | 세 표를 맨 앞에서 통째로, 순서 고정 |
| 3 | 보안 S7 이 전체 계정에 BCrypt 를 요구해 **탈퇴 계정(해시 NULL)** 때문에 거짓 실패 | 살아 있는 계정만 요구하고, 탈퇴 계정은 반대로 "비어 있는가" 를 본다 |
| 4 | `UserDeleteServiceTest` 가 주문보다 전자책을 먼저 지웠다 | 순서 교정 |
| 5 | `PrintOrderServiceTest` 가 파일 키를 하드코딩해 테스트끼리 겹쳤다 | 키에 userId 를 넣어 유일하게 |
| 6 | `ManuscriptImportTest` 가 다른 검증이 남긴 원고를 상한 계산에 함께 셌다 | 쓰는 자리만 비우고 시작 |

그리고 채점에서 수 하나를 뺐다 — `refresh_token` 전체 행 수는 로그인마다 늘고 만료+24h 가
지나야 줄어 **두 번 돌리면 값이 달라진다**(실제로 1,911 vs 1,919 로 어긋났다).
채점은 두 실행이 완전히 같아야 하므로 그런 수를 넣지 않는다.

---

## 미사용 코드를 지웠다

챕터 7·8 은 "곧 쓰일 자리" 를 남겨 뒀다. 챕터 9 가 마지막 코드 챕터라 그 "곧" 이 더는 오지 않는다.
**ErrorCode 12개 · 매퍼 메서드 6개 · `Manuscript.isMission()`** 을 지웠고,
저장소의 미참조 코드가 **0** 이 됐다. 지우기 전에 변수명까지 붙여 다시 셌다 —
단순 grep 은 이름이 겹치는 메서드를 못 가른다(챕터 7 에서 그렇게 한 번 속았다).

`CertificateMapper.updateFileKey` 는 챕터 7 부터 비워 둔 자리인데 **이번에 채워졌다**(인증서 PDF).

---

## 배포 전에 알아야 할 것

| 항목 | 내용 |
|---|---|
| 한글 폰트 | `NotoSansKR-Regular.ttf`(6.1 MB)가 jar 에 들어간다. **빼면 PDF 한글이 □ 로 나간다** — 예외도 경고도 없이 |
| `ebook.public-base-url` | 인증서 QR 이 가리키는 주소. **운영 도메인으로 바꾸지 않으면 QR 이 localhost 를 가리킨다** |
| 저장소 | 운영은 S3. `ObjectStorageClient` 의 네 메서드만 바꾸면 되고 호출부는 그대로다 |
| 청소기 | **단일 인스턴스 전제.** 여러 대로 늘리기 전에 분산 잠금(ShedLock) |
| 결제 | 없다. `print_price` 는 설정값이고 "관리자 확인 = 입금 확인" 이다 |

---

## 남은 [질문]

1. 결제 연동 — 붙으면 `CONFIRMED` 앞에 `PAID` 가 하나 더 필요하다
2. 다중 인스턴스 — 늘리기 전에 ShedLock. 지금 넣으면 쓰지 않는 표가 하나 는다
3. 폰트 6 MB — 배포 크기가 문제되면 글리프 서브셋
4. 마지막 로그인 — 별도 표가 맞다. `users` 에 칸을 더하면 로그인마다 그 행을 쓴다
5. 탈퇴 유예 기간 — 챕터 1 보강부터 열려 있다(현행: 유예 없이 즉시 익명화)

---

## 이전 회차 — 전체 점검 E (챕터 0~8)

```
API 86(✅84 ⚠2 ❌0) · 기능 ✅49 ⚠0 ❌0 · newman 427/427 ×2 · JUnit 222/222
DB H1~H15 · 보안 S1~S21(S22 ➖ 대상 API 없음) · 도구 4/4 + 개수 단언
```

- 도구 자가검증 4/4 뒤에 **개수 단언**을 새로 넣었다 — 이 도구들이 틀리는 방식은 늘
  "실패가 아니라 분모가 조용히 줄어드는" 것이었다.
- 그때 보고한 제품 결함 셋이 이번 챕터에서 닫혔다 — `refresh_token` 누적(청소기),
  관리자 사용자 목록 부재(§6), 인쇄 주문 단건 조회·취소 미검증(폴더 E).

상세는 `backend/docs/audit/verify-ch0-8-결과_노션용.md`.
