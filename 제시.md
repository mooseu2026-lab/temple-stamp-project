# temple-stamp 백엔드 — 구현 기능·API 전량 + 정밀 점검 결과

작성 2026-09-07 · **챕터 0~10 완료 · 최종 점검 F 통과** — 배포 진입 **12/12** · 명세 매칭율 **92%**
`Java 21 · Spring Boot 3.5.16 · MyBatis 3.0.5 · MySQL 8 · Lombok · 응답 DTO 는 record`

```
표          36개 · 매퍼 XML 36개
엔드포인트  91개 (컨트롤러 34개) — ✅ 91 · ⚠ 0 · ❌ 0
기능        58개 — ✅ 58 · ⚠ 0 · ❌ 0
테스트      JUnit 279/279 · newman 요청 552 / 단언 2562 실행 · 실패 0 (2회 완전 일치)
점검        런타임 5xx 0 · 보안 S1~S25 실패 0 · DB H1~H18 · 미참조 코드 0
배포 전     첫 판정의 ❌ 셋은 챕터 9 보강에서, 감사 G·H 의 필수 3 + ⛔ 5 는 G 보강·챕터 10 에서 닫혔다
비로그인    91개 토큰 없이 실측 — 허용 17 · 보호 74 · 어긋남 0
시드        사찰 110 · 코스 12("○○ 공양의 길") · 자리 60 · 후보 213 · 참배요소 342 (전부 DRAFT)
좌표        미확보 1곳(봉인사)
```

> 자세한 실측은 `backend/docs/audit/verify-ch0-9-결과_노션용.md`(최종 점검 F)·`ch9-ebook.md`,
> 한 장 요약은 `결과.md`, 인수인계는 `audit/인수인계_temple-stamp_최종.md`.

---

# 1부 — 구현된 기능

## 1. 인증·계정 (챕터 1)

| 기능 | 내용 |
|---|---|
| 가입·로그인 | 이메일+비밀번호. 비밀번호는 영문·숫자·특수문자 각 1자 이상, 8~50자 |
| 액세스 토큰 | JWT HS256, 30분 |
| 재발급 | **불투명 랜덤 refresh 토큰** — DB 에는 SHA-256 해시만, 전달은 HttpOnly 쿠키. 쓸 때마다 회전 |
| 재사용 탐지 | 이미 쓴 쿠키가 다시 오면 그 사용자의 **전 기기 토큰을 폐기**한다 |
| 계정 잠금 | 로그인 5회 실패 → 15분 잠금. 잠긴 뒤에는 맞는 비밀번호도 403 |
| 정보 은닉 | 없는 계정과 비밀번호 오류가 **같은 401·같은 문구** — 계정 존재 여부가 새지 않는다 |
| 로그아웃 | 비로그인으로 들어와도 쿠키만 지우고 200 |

## 2. 회원 (챕터 2)

내 정보 조회·수정(닉네임·계층·언어·알림), 약관 동의 조회/추가/철회.
계층(`tier`)은 `AGE20·AGE30·AGE40·AGE50·AGE60·RIDER·FOREIGN` — 문구·미션 배정 기준이다.
**위치기반서비스 동의는 GPS 인증의 선행 조건**이다(없으면 403 `USER-4031`).

## 3. 마스터 조회 (챕터 2)

| 기능 | 내용 |
|---|---|
| 권역 | 10개(서울·경기인천·강원·충남세종·충북·전북·전남·경북·경남·제주). 코스를 담은 권역은 9개 |
| 코스 | 권역별 목록·상세. 상세는 **사찰 5곳의 좌표·반경·QR 힌트를 한 번에** 준다(산중에서 다시 네트워크를 타지 않게) |
| 사찰 | 기본 정보 · **싱글페이지 8블록** · **「가는 법」 7칸** |
| 오관게 | 5구 목록·상세 (한자·한글·주제) |
| 콘텐츠 | 사찰 예절 안내문 · 다국어 문자열 묶음 |

**싱글페이지**는 계층·언어에 따라 확장문구와 미션이 달라진다. `?target=` 이 `users.tier` 를 덮고,
`?locale=` 이 `Accept-Language` 를 덮는다.

**「가는 법」**은 일주문→금강문→천왕문→불이문→탑→주불전→부속전각 **7칸이 언제나 다 나온다.**
없는 자리도 빠지지 않고 `present:false` 로 자리를 지킨다(절마다 화면 구성이 달라지지 않도록).
각 자리는 자기 '오는 길' 문장을 **한 번씩만** 낸다.

## 4. 순례·여권 (챕터 4)

코스 시작(멱등 — 두 번 눌러도 한 건), 여권 조회(권역→코스→칸 3단 + 요약 4수치), 진행률.
진행률은 목록·상세 응답에 실려 나가고 **계산은 한 곳(`PilgrimageService.progressOf`)** 에서만 한다.

## 5. 현장 인증 3단계 (챕터 5) ★

```
① GPS 도착 확인 → ② 사찰 QR 검증 → ③ 다짐 제출 → 도장 발급
   (①부터 60분 안에 끝내야 한다. 넘기면 EXPIRED)
```

| 단계 | 지키는 것 |
|---|---|
| ① GPS | **서버는 좌표를 받지 않는다.** 단말이 "반경 안인가"·"정확도 등급"만 보낸다. LOW 는 저장조차 안 됨 |
| ① v4 | 한 자리에 후보 사찰이 여럿 — `siteId` 로 어디에 서 있는지 받고, 그 자리의 후보가 아니면 거절 |
| ② QR | 로그인 키와 **다른 키**로 서명. `site_id` + `qr_version` 이 토큰에 박힌다. 만료 없음 — 회전으로만 무효화 |
| ③ 미션 | 계층별 미션 배정 · 다짐 10~300자 · 사진 선택 |
| 이동시간 | 직전 도장에서 최소 이동시간을 못 채우면 완료가 아니라 **심사 보류**(PENDING·TRAVEL_TIME) |
| 한도 | 하루 5개(살아 있는 도장 기준) · 예외 접수 하루 2건 |
| 예외 접수 | GPS·QR 이 불가능한 상황 — 사진+설명으로 접수, 관리자 승인 대기 |
| 재발행 방지 | 같은 자리 두 번째는 409 + **"이미 발행된 스탬프입니다 — {사찰} · {날짜}"** |

## 6. 도장 이후의 연쇄 (챕터 7 코드, 챕터 5 흐름에서 실제로 돈다)

도장 확정 → 보상 적립 → 5칸이 차면 **코스 완주**(인증서 발급·전자책 대기열·코스 보상)
→ 완주 3코스면 중간본 → **전 코스 완주면 회향 인증서·회향본·회향 보상**.
심사 반려로 완료가 줄면 완주가 취소되고 **인증서는 회수**된다(보상은 사람이 판단).

## 7. 생각상자·명상·사진·업로드

생각상자(직접 작성 / 미션에서 자동 사본), 명상 콘텐츠·재생 기록, 업로드 presign(용도별 키 소유 검증).

## 8. 전자책·인증서·보상

전자책 목록·다운로드 URL, 인쇄 신청·조회·취소, 인증서 목록·**공개 진위 확인**, 보상 목록·수령 신청.

## 9. 관리자

사찰 CRUD·공개 전환·뷰포인트·뱃지·**참배 요소(가는 법 7자리)**·QR 발급/회전,
코스 CRUD·공개 전환·이동시간 표, 카카오 장소 검색, **스탬프 심사**, 보상 수령 심사,
전자책 파일 등록·인쇄 주문 관리, 확장문구·미션 원고 등록, **원고 심사·CSV 일괄 반입(챕터 8)**,
**사용자 목록·청소기 수동 실행(챕터 9)**.

## 12. 전자책 · 인증서 PDF · 인쇄 · 청소기 (챕터 9) ★

손에 쥘 수 있는 것이 처음으로 나온다. 지금까지 기록은 API 응답으로만 존재했다.

- **전자책** 개인 소장본 PDF. 도장·사진·문장·그날의 확장문구·생각상자(비공개 포함)·명상·인증서
- **스냅샷 해시** 재료를 정렬해 SHA-256 — 같으면 같은 책이라 버튼을 눌러도 쌓이지 않는다
- **비동기 조판** 요청은 202 로 받고 청소기(5분)가 만든다. 웹 스레드를 붙들지 않는다
- **한글 폰트 임베드** 안 넣으면 □ 로 나가는데 예외가 없다 → JUnit 이 PDF 에서 텍스트를 도로 뽑아 확인
- **사진 1200px·JPEG 80** 원본 그대로면 60장에 수백 MB
- **인증서 PDF** 첫 조회 때 생성 · 진위 확인 QR · VALID 일 때만 링크 · 회수해도 파일은 남긴다
- **인쇄주문** 상태 여섯. PRINTING 부터는 취소 없음. 전이는 UPDATE 의 WHERE 절에 있다
- **배송정보 분리** 목록 질의가 주소를 읽지 않는다 — 가리는 것이 아니라 없다
- **청소기 하나** 파일 삭제 큐 · 만료 토큰(만료+24h) · 전자책 조판 · 도장 세션. 작업마다 트랜잭션 독립
- **관리자 사용자 목록** 기본 ACTIVE · 해시·토큰·주소 없음

## 11. 원고 — 편집자·심사·CSV 반입 (챕터 8) ★

사찰 × 오관게 구에 붙는 글을 사람이 쓰고, 다른 사람이 보고, 내보낸다.

- **상태 기계** DRAFT → SUBMITTED → APPROVED → RETIRED (반려는 사유 필수, 수정하면 DRAFT 로 복귀)
- **4-eyes** 자기 원고는 자기가 승인·반려하지 못한다. 관리자도 원고를 쓸 수 있어 실제로 걸린다
- **삭제 없음** 퇴역만. 이미 그 원고를 참조하는 도장의 글이 사라지면 안 되기 때문이다
- **금칙 4종** 링크·전화번호·이메일·HTML 태그는 지우지 않고 **거부**한다
- **변형 상한** 사찰당 3편, 기본 원고(site_id NULL)는 구·종류마다 1편 — 생성 컬럼 유니크가 DB 에서 막는다
- **선택 규칙** 후보[userId mod 후보 수]. 난수가 아니라 나머지라 같은 사람은 늘 같은 글을 본다
- **세션 고정** 고른 원고를 도장 행에 못 박는다. 읽는 중에 퇴역·교체돼도 화면의 글은 안 바뀐다
- **CSV 반입** 행 단위 검사 · **파일 단위 원자성**(한 행이라도 틀리면 0건) · dryRun · 동명이찰 대조

## 10. v4 데이터화 (챕터 4 마감)

조사 CSV(사찰 111 · 후보 215)를 시더로 적재. `site.seed_key` 로 **멱등**(두 번 돌려도 같은 행 수).
좌표는 카카오 검색으로 채우고 못 찾은 18곳은 `(0,0)` 으로 두어 공개 전환이 막히게 한다.

---

# 2부 — API 전량 (91개)

> 아래 목록은 챕터 0~5 시점의 69개다. 챕터 6·7·보강에서 여덟, 챕터 8 에서 아홉, 챕터 9 에서 다섯이 늘었다 —
> `PUT/GET /api/photos/{siteId}` · `GET /api/photos` · `GET /api/thinkbox/flashback` ·
> `POST /api/admin/completions/recount`(1명·배치) · `POST /api/admin/certificates/{id}/revoke` · `DELETE /api/users/me`(회원 탈퇴).
> 챕터 8 의 아홉은 아래 「편집자」·「관리자」 표에 있다.
> **정본은 `backend/docs/audit/endpoints.md`** 이고 거기에 newman 요청 id 와 판정이 함께 있다.

응답은 전부 `{success, data, error, timestamp}` 봉투. 오류는 `error.code`·`error.message`,
검증 오류는 `error.fields[]`. 모든 응답에 `X-Request-Id`(32자).

## 공개 (인증 불필요)

| 메서드 | 경로 | 요청 | 설명 |
|---|---|---|---|
| POST | `/api/auth/signup` | email, password, nickname | 가입 |
| POST | `/api/auth/login` | email, password | 로그인 → accessToken + refresh 쿠키 |
| POST | `/api/auth/refresh` | (쿠키) | 회전 재발급 |
| POST | `/api/auth/logout` | (쿠키) | 전 기기 폐기 |
| GET | `/api/regions` | — | 권역 목록 (`courseCount` 포함) |
| GET | `/api/courses` | `?regionId` | 코스 목록 |
| GET | `/api/courses/{courseId}` | `?target` | **코스 상세** — 자리 5칸 + 좌표·반경·힌트 + `candidates[]` |
| GET | `/api/sites/{siteId}` | — | 사찰 기본 정보 |
| GET | `/api/sites/{siteId}/page` | `?target&locale` | 싱글페이지 8블록 |
| GET | `/api/sites/{siteId}/guide` | — | 「가는 법」 7칸 |
| GET | `/api/verses` · `/api/verses/{verseNo}` | — | 오관게 5구 |
| GET | `/api/meditations` · `/{meditationId}` | — | 명상 콘텐츠 |
| GET | `/api/content/guide` · `/api/content/i18n/{lang}` | — | 예절 안내 · 다국어 문자열 |
| GET | `/api/certificates/verify/{serialNo}` | — | 인증서 진위 확인 |
| GET | `/health` | — | 헬스체크 |

## 로그인 사용자

| 메서드 | 경로 | 요청 | 설명 |
|---|---|---|---|
| GET | `/api/users/me` | — | 내 정보 |
| PATCH | `/api/users/me` | nickname, tier, locale, notificationEnabled | null 은 "변경 없음" |
| GET | `/api/users/me/agreements` | — | 동의 목록 |
| POST | `/api/users/me/agreements` | **배열** `[{agreementType, version}]` | 동의 (재동의는 최초 시각 유지) |
| DELETE | `/api/users/me/agreements/{agreementType}` | — | 철회 |
| GET | `/api/passport` | — | 여권 (권역→코스→칸 + 요약) |
| POST | `/api/pilgrimages` | courseId | 순례 시작 (멱등) |
| **POST** | **`/api/stamps/{courseSiteId}/gps-check`** | **siteId, withinRadius, accuracyGrade** | ① 도착 확인 |
| **POST** | **`/api/stamps/{stampId}/qr`** | qrToken | ② QR 검증 |
| **POST** | **`/api/stamps/{stampId}/mission`** | sentence, photoKey?, hasOtherFace?, expansionPhraseId? | ③ 다짐 → 발급 |
| POST | `/api/stamps/evidence` | courseSiteId, **siteId**, sentence, photoKey | 예외 접수 |
| GET | `/api/stamps/{stampId}` | — | 진행 상태(앱 복귀용) |
| **GET** | **`/api/stamps/by-slot/{courseSiteId}`** | — | 그 자리에서 받은 완료 도장 |
| POST | `/api/thinkbox` | content, courseId?, siteId? | 생각상자 작성 |
| GET | `/api/thinkbox` | `?page&size` | 내 글 목록 |
| PATCH | `/api/thinkbox/{thinkboxId}` | content, isPublic | 수정 |
| DELETE | `/api/thinkbox/{thinkboxId}` | — | 삭제 |
| POST | `/api/meditations/logs` | meditationId, playedSeconds, memo? | 재생 기록 |
| GET | `/api/rewards` | — | 내 보상 |
| POST | `/api/rewards/{userRewardId}/claim` | — | 수령 신청 |
| GET | `/api/certificates` | — | 내 인증서 |
| GET | `/api/ebooks` | — | 내 전자책 |
| GET | `/api/ebooks/{ebookId}/download-url` | — | 다운로드 URL |
| POST | `/api/print-orders` | ebookId, quantity, recipientName, recipientPhone, postalCode, address, addressDetail?, note? | 인쇄 신청 |
| GET | `/api/print-orders` · `/{printOrderId}` | — | 신청 조회 |
| DELETE | `/api/print-orders/{printOrderId}` | — | 신청 취소 |
| GET | `/api/uploads/presign` | `?purpose&contentType` | 업로드 URL |

## 전자책·인쇄 (챕터 9 · 로그인)

| 메서드 | 경로 | 요청 | 설명 |
|---|---|---|---|
| **POST** | **`/api/ebooks`** | — | 만들기 요청. **202** REQUESTED / 같은 재료면 **200** 그 책 |
| GET | `/api/ebooks` | — | 내 목록(최신순). 링크는 붙지 않는다 |
| **GET** | **`/api/ebooks/{id}`** | — | 1권. `downloadUrl` 은 READY 일 때만(10분), 아니면 null |
| GET | `/api/ebooks/{id}/download-url` | `?format=PDF` | 서명 URL 단독 발급 |
| **GET** | **`/api/certificates/{id}`** | — | 인증서 1장. VALID 일 때만 `downloadUrl`. 첫 조회에 PDF 생성 |
| POST | `/api/print-orders` | ebookId, quantity 1~5, 배송정보 | READY 전자책만. 아니면 400 `PRINT-4001` |
| GET | `/api/print-orders` | — | 목록 — **배송정보 키가 없다** |
| GET | `/api/print-orders/{id}` | — | 1건 — `shipping{...}` 포함. 남의 것 404 |
| DELETE | `/api/print-orders/{id}` | — | **204**. REQUESTED 에서만, 행은 남는다 |

## 편집자 (`ROLE_EDITOR` · 관리자도 쓸 수 있다)

| 메서드 | 경로 | 요청 | 설명 |
|---|---|---|---|
| POST | `/api/editor/manuscripts` | siteId?, verseNo, kind, title, body | 등록 → 201 DRAFT. `variantNo` 는 **서버가 매긴다** |
| PUT | `/api/editor/manuscripts/{id}` | title, body | 작성자 본인 · DRAFT·REJECTED 만. 남의 것은 **404** |
| POST | `/api/editor/manuscripts/{id}/submit` | — | 제출 |
| GET | `/api/editor/manuscripts` | `?siteId&verseNo&kind&status&page&size` | 편집자는 **자기 것만**, 관리자는 전체 |

## 관리자 (`ROLE_ADMIN`)

| 메서드 | 경로 | 요청 | 설명 |
|---|---|---|---|
| GET | `/api/admin/sites` | `?q&page&size` | 사찰 목록 (DRAFT 포함) |
| POST · PUT | `/api/admin/sites` · `/{siteId}` | latitude, longitude, verifyRadius, qrLocationHint?, parkingInfo?, accessInfo?, mealAvailable?, i18n[] | 등록·수정 (**본문에 `status` 금지**) |
| PATCH | `/api/admin/sites/{siteId}/status` | status | 공개 전환 |
| POST | `/api/admin/sites/{siteId}/viewpoints` | sortNo, locationDesc, bestTime, whatToSee | 뷰포인트 UPSERT |
| POST | `/api/admin/sites/{siteId}/badges` | badgeType, description | 뱃지 UPSERT |
| **POST** | **`/api/admin/sites/{siteId}/elements`** | `items[{elementCode, localName?, note?}]` | 참배 요소 UPSERT |
| **DELETE** | **`/api/admin/sites/{siteId}/elements/{elementCode}`** | — | 요소 삭제 |
| POST | `/api/admin/sites/{siteId}/qr` | `?validitySeconds` | QR 발급 (`qrToken`·`qrImageBase64`) |
| POST | `/api/admin/sites/{siteId}/qr/rotate` | — | 버전 +1 (기존 QR 전부 무효) |
| POST · PUT | `/api/admin/courses` · `/{courseId}` | regionId, name, description?, status?, sortNo?, sites[5] | 코스 등록·수정 |
| PATCH | `/api/admin/courses/{courseId}/status` | status | 코스 공개 전환 |
| PUT | `/api/admin/site-distances` | items[] | 이동시간 표 UPSERT |
| GET | `/api/admin/stamps/pending` | `?page&size` | 심사 대기 목록 |
| POST | `/api/admin/stamps/{stampId}/review` | decision, reason? | 승인/반려 |
| GET | `/api/admin/rewards/claims/pending` | — | 보상 수령 심사 목록 |
| POST | `/api/admin/rewards/claims/{userRewardId}/review` | decision, reason? | 보상 심사 |
| GET | `/api/admin/kakao/places` | `?query&page&size` | 장소 검색 |
| POST | `/api/admin/contents/phrases` | verseNo, tier, versionNo, textKo, reviewStatus | 확장문구 등록 |
| POST | `/api/admin/contents/missions` | verseNo, tier, variantNo, body, originRef, reviewStatus | 미션 등록 |
| PUT | `/api/admin/ebooks/{ebookId}/files` | pdfKey, epubKey | 전자책 파일 등록 |
| GET | `/api/admin/ebooks/print-orders` | `?page&size` | 인쇄 주문 목록 |
| PATCH | `/api/admin/ebooks/print-orders/{printOrderId}/status` | status, trackingNo? | 주문 상태 |
| **GET** | **`/api/admin/manuscripts`** | `?status&siteId&verseNo&kind&page&size` | 원고 심사 목록 |
| **POST** | **`/api/admin/manuscripts/{id}/approve`** | — | 승인 (자기 원고면 403 `MS-4030`) |
| **POST** | **`/api/admin/manuscripts/{id}/reject`** | reason | 반려 (사유 필수) |
| **POST** | **`/api/admin/manuscripts/{id}/retire`** | — | 퇴역 — **삭제가 아니다** |
| **POST** | **`/api/admin/manuscripts/import`** | multipart `file` · `?dryRun` | CSV 일괄 반입 (전부 DRAFT) |
| **GET** | **`/api/admin/users`** | `?status&q&page&size` | 기본 ACTIVE. 해시·토큰·주소 없음 (챕터 9) |
| **POST** | **`/api/admin/housekeeping/run`** | — | 청소기 한 바퀴. 응답에 처리 건수 (챕터 9) |

---

# 3부 — 정밀 점검 결과

> **2026-09-07 최신**: 챕터 9 — 기능 ✅58 ⚠0 ❌0 · API 91(✅91 ⚠0 ❌0 — 미검증이 사라졌다) ·
> newman 515/2411 2회 일치 · JUnit 260/260 · 보안 S1~S22 실패 0 · DB H1~H16 · 미참조 코드 0.
> 이번 회차에 잡은 제품 결함 넷: 저장소가 바이트를 저장하지 않았다 · 탈퇴 연쇄가 FK 로 500 ·
> 하루 상한이 완주 코스본까지 세었다 · 만료 토큰 정리가 한 번도 지운 적이 없었다(결과.md).
> 아래 3부는 챕터 5 시점의 정밀 점검 기록이며 A·B 항목은 그때 전부 수정됐다.
> 남은 ⚠ 는 **없다** — 회원 탈퇴는 챕터 1 보강에서 만들었고, 시간 기반 기능은 JUnit 실측을 근거로 인정하기로 정했다.
> 아래는 챕터 5 시점의 정밀 점검 기록이며 A·B 항목은 그때 전부 수정됐다.

> **2026-09-06 후속**: 아래 A-1·A-2·B-1·B-2·B-3·B-5 는 **전부 수정됐다** —
> `backend/docs/audit/ch5-hardening.md`. B-4 는 배포 체크리스트(정리.md §5-2 9번)로 넘겼다.
> 남은 것은 C 절(콘텐츠·챕터 6~9)뿐이다. 아래 본문은 발견 당시 기록으로 그대로 둔다.

## 점검 방법

1. **매퍼 인터페이스 ↔ XML 문장 id 전수 대조** — 불일치 0건 (있으면 호출 시점 500)
2. **XML `#{param}` ↔ `@Param` 이름 전수 대조** — 불일치 0건
3. **`resultType` 클래스 필드 ↔ SELECT 별칭 전수 대조** (70개 select) — **채워지지 않는 필드 0건**
   (의견91 때 잡았던 "조용히 null" 부류가 남아 있지 않다)
4. **실기동 후 69개 엔드포인트 중 주요 경로 직접 호출** — 5xx 0건
5. **코스 완주 → 회향 연쇄를 실제로 태워 봄** (아래 A-1 이 여기서 나왔다)
6. 트랜잭션 경계·`@Validated` 누락·잠금 순서 정적 점검

## A. 지금 실행하면 잘못 동작한다

### A-1. ⚠⚠ 코스 **하나만** 끝내도 "전 코스 회향" 이 발급된다 — 실물 보상까지 나간다

`CompletionService` 는 회향 조건을 `completedCourses >= courseService.countActiveCourses()` 로 판정한다.
그런데 **지금 ACTIVE 코스는 데모 1개뿐**이다(시드 12코스는 전부 DRAFT). 그래서 코스 1을 끝내는 순간
`1 >= 1` 이 참이 되어 회향까지 한꺼번에 터진다.

**실제로 태워 본 결과** (사용자 하나, 코스 1의 5칸을 채움):

```
rewards: [ 순례 도장 · 코스 완주 쿠폰 · 회향 기념품(PHYSICAL, claimable:true) ]
certificate: PILGRIMAGE PG-2026-000001  +  HOEHYANG HH-2026-000001
ebook:       PILGRIMAGE(QUEUED)         +  HOEHYANG(QUEUED)
```

**"회향 기념품" 은 `claimable:true` 인 실물 보상이다.** 지금 상태로 문을 열면 첫 코스를 끝낸 사람마다
회향 인증서와 실물 기념품 신청권이 나간다. 인증서 번호가 이미 제3자에게 제시됐을 수 있어 회수도 지저분하다.

- **왜 이렇게 됐나** — 챕터 4 마감에서 `TOTAL_COURSES = 12` 상수 대신 `countActiveCourses()` 를 택했다.
  "코스를 하나 내리면 회향이 조용히 안 된다" 는 문제는 그렇게 피했지만, **반대 방향(코스가 적을 때)** 은 막지 않았다.
- **고치는 방법 세 가지**
  1. **하한을 둔다** — `if (totalCourses >= HOEHYANG_MIN_COURSES && completedCourses >= totalCourses)`.
     `HOEHYANG_MIN_COURSES` 를 설정값으로 빼고 기본 12. 데이터가 덜 찼을 때 회향이 안 나가는 쪽이 안전하다.
  2. 시드 12코스를 ACTIVE 로 올린다 — 사찰 110곳의 QR 힌트가 필요해 지금은 불가능하다.
  3. 회향을 자동 발급하지 않고 관리자 승인 뒤에 낸다.
- **권장은 1번** — 한 줄이고 지금 바로 막힌다.

### A-2. `StampController` 의 `@Positive` 가 통째로 무시되고 있다

클래스에 **`@Validated` 가 없다.** 파라미터 제약은 클래스에 그 어노테이션이 있어야만 검사된다
(정리.md §6-6 에 이미 적혀 있는 함정인데 이 컨트롤러만 빠졌다). 실측:

```
GET /api/stamps/-5        → 404 STAMP-4040     (400 COMMON-4000 이어야 한다)
GET /api/verses/9         → 400 COMMON-4000    ← 형제 컨트롤러는 정상
GET /api/thinkbox?page=-1 → 400 COMMON-4000
```

터지지는 않지만 **같은 종류의 잘못된 입력에 API 마다 다른 코드가 나간다.** 프론트가 분기해야 한다.
`@Validated` 한 줄이면 끝난다. 다만 그 순간 응답 코드가 404→400 으로 바뀌므로 컬렉션 단언도 같이 봐야 한다.

## B. 조건이 맞으면 터진다 (아직 그 조건이 안 왔을 뿐)

### B-1. `PhraseService` 가 `readOnly = true` 인데 두 메서드가 쓴다

```java
@Transactional(readOnly = true)
public class PhraseService {
    public Mission pickMission(...) { ...  taskSeenMapper.markSeen(...); }   // 쓴다
    public void markPhraseSeen(...) {      phraseSeenMapper.markSeen(...); } // 쓴다
```

지금은 둘 다 `StampService` 의 쓰기 트랜잭션 안에서 불려 **바깥 트랜잭션에 합류**하므로 통과한다.
읽기 전용 트랜잭션에서 부르거나 트랜잭션 없이 부르는 **호출자가 하나만 생기면** 그 순간
`Connection is read-only` 로 500 이다. 메서드에 `@Transactional` 을 붙이면 된다.

### B-2. 완주 취소 → 재승인이면 중간본이 두 번 쌓인다

```java
if (completedCourses == INTERIM_COURSE_COUNT) { ebookService.enqueue(userId, null, Ebook.INTERIM); ... }
```

인증서·보상은 멱등하게 만들어져 있는데(`findByPilgrimageId().orElseGet(...)`, `grant()` 가 0행이면 건너뜀)
**전자책 대기열만 그 보호가 없다.** 반려로 완주가 취소됐다가 다시 승인되면 3코스 중간본이 한 번 더 큐에 들어간다.
회향본도 같다. `ebook` 에 (user, type) 유니크를 두거나 enqueue 를 멱등하게 바꾸면 된다.

### B-3. 하루 한도가 "어제 시작한 도장" 을 세지 않는다

한도는 `created_at >= CURDATE()` 기준이다. 어제 GPS 만 찍고 만료된 행은 `findOrCreate` 가 **재사용**하는데
그때 `created_at` 은 어제 그대로다. 즉 어제 만든 행으로 오늘 도장을 받으면 그 한 건은 오늘 한도에 안 잡힌다.
악용은 어렵지만(자리마다 한 행) 규칙과 실제가 다르다.

### B-4. `CURDATE()` 는 MySQL **서버 OS 시계**를 따른다

`@@global.time_zone` 이 `SYSTEM` 이다. 이 장비는 OS 가 KST 라 결과적으로 맞지만,
**OS 가 UTC 인 서버에 올리면 하루 한도 초기화가 오전 9시로 밀린다.** JDBC 의 `serverTimezone=Asia/Seoul`
은 드라이버의 해석 기준일 뿐 서버를 바꾸지 않는다. → 정리.md §5-2 배포 체크리스트 9번.

### B-5. 증빙 접수가 실패하면 `EXPIRED` 행이 남을 수 있다

`findOrCreateForEvidence` 는 행을 먼저 `EXPIRED` 로 만들고(그 상태라야 `markPending` 이 받아 준다)
그다음 `markPending` 을 친다. 같은 트랜잭션이라 예외가 나면 함께 롤백되지만,
**"시작 상태로 EXPIRED 를 쓴다" 는 것 자체가 읽는 사람을 속인다** — 만료된 적이 없는데 만료로 보인다.
`PENDING` 을 처음부터 넣도록 `insertEvidence` 를 따로 두는 편이 낫다.

## C. 설계상 알고 있어야 하는 것 (버그 아님)

| # | 내용 |
|---|---|
| C-1 | **시드 사찰 110곳·코스 12개가 전부 DRAFT** 다. `qr_location_hint` 가 비어 있어 공개 전환이 막힌다(`ADMIN-4092`). 지금 사용자에게 열려 있는 코스는 데모 1개뿐이다 |
| C-2 | 좌표가 `(0,0)` 인 사찰 18곳 — 반경 150m 안에서만 도장이 나가므로 그대로 열면 아무도 인증 못 한다. 공개 전환 전에 채워야 한다 |
| C-3 | 과포화 4곳(통도사 서운암·해인사·범어사·백양사)은 `congested:true` 로 나간다. 부산·동부 1·2구는 후보가 그것뿐이라 그 자리는 전부 "혼잡" 이다 |
| C-4 | `STAMP-4003`(QR 유효시간)은 **미사용(예약)** 이다. QR 은 만료 없이 회전으로만 무효화한다 |
| C-5 | 사진 1장·문장 1개 개인 소장(`PUT /api/photos/{siteId}`)은 **챕터 6** 이라 아직 없다 |
| C-6 | 보상·인증서·전자책·생각상자·명상은 코드가 있고 실기동에서 200 이지만 **종단 테스트가 없다**(챕터 6~9) |

## D. 확인했고 문제 없던 것

- 매퍼 ↔ XML, `#{param}` ↔ `@Param`, `resultType` ↔ SELECT 별칭 — **전부 일치**
- 보호 경로 매처: `/api/admin/**` = `ROLE_ADMIN`, GET 공개 목록은 메서드까지 제한, 나머지 `authenticated()`
- 인증서 발급·보상 적립은 **멱등**
- 잠금 순서 `pilgrimage → stamp` — 사용자 제출과 관리자 승인을 실제 두 스레드로 동시에 태워 교착 없음
- 60분 만료가 DB 에 `EXPIRED` 로 남는다(REQUIRES_NEW 실측)
- 검증 세트 2회 연속 같은 값 — 멱등
- 기동 로그에 경고·오류 없음

---

## 우선순위

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | **A-1 회향 오발급** | 문을 여는 순간 실물 보상이 잘못 나간다. 되돌리기 어렵다 |
| 2 | C-1·C-2 (QR 힌트·좌표) | 이게 없으면 시드 12코스를 못 연다 — 서비스가 성립하지 않는다 |
| 3 | B-1 `readOnly` | 호출자 하나만 늘면 500. 고치는 비용이 거의 없다 |
| 4 | A-2 `@Validated` | 응답 일관성. 프론트 분기를 줄인다 |
| 5 | B-2 전자책 중복 | 운영 중 심사 반려가 생기면 그때 드러난다 |
| 6 | B-3·B-4·B-5 | 규칙과 실제의 어긋남. 지금 당장 사고로 이어지지는 않는다 |
