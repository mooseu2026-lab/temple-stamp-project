<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.templestamp.meditation.MeditationLogMapper">

    <insert id="save" parameterType="com.templestamp.meditation.MeditationLog"
            useGeneratedKeys="true" keyProperty="meditationLogId">
        INSERT INTO meditation_log (user_id, meditation_id, played_sec, memo)
        VALUES (#{userId}, #{meditationId}, #{playedSec}, #{memo})
    </insert>

    <select id="findByUserId" resultType="com.templestamp.meditation.MeditationLog">
        SELECT meditation_log_id, user_id, meditation_id, played_sec, memo, created_at
        FROM meditation_log
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <delete id="deleteByUser">
        DELETE FROM meditation_log WHERE user_id = #{userId}
    </delete>

</mapper>
