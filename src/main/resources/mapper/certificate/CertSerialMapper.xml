<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.templestamp.certificate.CertSerialMapper">

    <!--
      INSERT ... ON DUPLICATE KEY UPDATE 한 문장이라 원자적이다.
      첫 발급이면 1, 그 뒤로는 직전 값 + 1. 되돌리는 문장은 두지 않는다 —
      회수해도 번호는 소비된 것이다(재사용 금지, §2-4).
    -->
    <update id="bump">
        INSERT INTO cert_serial (cert_type, issue_year, next_seq)
        VALUES (#{certType}, #{year}, 1)
        ON DUPLICATE KEY UPDATE next_seq = next_seq + 1
    </update>

    <select id="current" resultType="int">
        SELECT next_seq FROM cert_serial
        WHERE cert_type = #{certType} AND issue_year = #{year}
    </select>

</mapper>
