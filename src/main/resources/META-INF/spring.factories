# EnvironmentPostProcessor 는 빈 스캔보다 먼저 도는 지점이라 @Component 로 등록되지 않는다.
# .env 를 못 찾았을 때 DataSource 가 엉뚱한 값으로 붙기 전에 멈추게 하려면 여기여야 한다.
org.springframework.boot.env.EnvironmentPostProcessor=\
com.templestamp.global.config.RequiredEnvCheck
