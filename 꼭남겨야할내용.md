# 꼭 남겨야 할 내용 — temple-stamp

다른 문서가 다 사라져도 **이것만 있으면 다시 세울 수 있는 것**만 담았다.
길게 쓴 것은 전부 다른 곳에 있다. 여기 있는 것은 *잃으면 다시 알아내는 데 며칠이 걸리는* 것들이다.

---

## 1. 이 저장소를 처음 띄우는 법 (4분)

```bash
# ① DB
CREATE DATABASE temple_stamp_project
  DEFAULT CHARACTER SET utf8mb4 DEFAULT COLLATE utf8mb4_unicode_ci;

# ② 프로젝트 루트에 .env.local (저장소에 없다. 직접 만든다)
DB_PASSWORD=...
JWT_SECRET=$(openssl rand -base64 32)
QR_SECRET=$(openssl rand -base64 32)     # JWT_SECRET 과 반드시 다른 값
STORAGE_SECRET_KEY=local-storage-secret
KAKAO_REST_API_KEY=                      # 비우면 좌표가 안 채워진다(아래 ★)

# ③ 시드까지 넣고 기동 — 이 프로파일이 핵심이다
SPRING_PROFILES_ACTIVE=seed ./gradlew bootRun

# ④ 그다음부터는 그냥
./gradlew bootRun

# ⑤ 검증 한 바퀴 (3~4분, 마지막에 failed 0 이면 통과)
bash postman/run-all.sh
```

**★ `SPRING_PROFILES_ACTIVE=seed` 없이 띄우면 사찰 5곳·코스 1개만 들어간다.** 그 상태로 `run-all.sh` 를 돌리면
단언 30건이 실패한다. 이 한 줄이 어느 문서에도 없어서 처음 보는 사람이 막히는 유일한 지점이었다(감사 G STEP 9).

**★ 카카오 키가 비면 시더가 CSV 로 물러나 좌표를 못 채운다** — 새로 세운 DB 는 좌표 없는 사찰이 106곳이다.
개발 DB 의 "미확보 1곳" 은 뒤에 카카오로 백필한 결과라, 그 숫자를 새 환경의 기대값으로 쓰면 안 된다.

**두 시크릿이 같으면 기동 자체가 막힌다.** QR 은 사찰 현장에 물리적으로 붙어 있어 사진 한 장으로도 새어 나가는데,
같은 키면 그 QR 하나로 로그인 토큰을 위조할 수 있다.

**윈도우 Git Bash 에서는 요청 본문에 한글을 직접 넣지 말 것.** argv 가 ANSI 로 바뀌어 `400 COMMON-4004` 가 난다.
`curl --data-binary @body/xxx.json` 처럼 파일로 넘긴다.

---

## 2. 배포 전에 반드시 하는 것

### 2-1. 운영 DB 에 ALTER 네 파일 — 순서대로

```bash
mysql -u<user> -p <스키마> < backend/docs/verify/alter-ch7.sql
mysql -u<user> -p <스키마> < backend/docs/verify/alter-ch1-withdraw.sql
mysql -u<user> -p <스키마> < backend/docs/verify/alter-ch8.sql
mysql -u<user> -p <스키마> < backend/docs/verify/alter-ch9.sql
```

`schema.sql` 은 운영에서 실행되지 않는다(`sql.init.mode=never`). 표 정의를 고쳐도 이미 있는 표에는 반영되지 않는다.
**이 네 파일이 운영 DB 를 지금 코드에 맞추는 유일한 길이다.**

맞게 올라갔는지는 `bash backend/docs/verify/db-check.sh all` 의 **H17** 이 확인한다 —
임시 스키마 둘(ALTER 사슬 / `schema.sql`)을 세워 전수 비교하고 지운다. **차이 0** 이 아니면 어긋난 것이다.

### 2-2. 빠뜨리면 조용히 망가지는 것 열둘

| # | 항목 | 빠뜨리면 |
|---|---|---|
| 1 | 위 ALTER 네 파일 | 색인이 옛것이거나 `cert_serial` 이 없어 **첫 완주가 나오는 날** 인증서 발행이 터진다 |
| 2 | 운영용 JWT·QR 시크릿 별도 발급 | 로컬 값이 운영에 남는다 |
| 3 | actuator 노출이 `health` 하나인지 — **`application.yml`·`application-prod.yml` 두 곳** | `base-path` 가 `/` 라 `/env` 가 루트에 열리고 거기 **DB 비밀번호·JWT 시크릿**이 있다 |
| 4 | `app.cookie.secure: true` | Secure 없는 쿠키가 나간다 |
| 5 | `cors.allowed-origins` 를 배포 도메인으로 | 로컬 기본값이 남는다. `allow-credentials` 가 true 라 `*` 불가 |
| 6 | `PUBLIC_BASE_URL` 운영 도메인 | **인증서 QR 이 localhost 를 가리킨 채 종이로 인쇄된다.** 지금은 기동이 막히게 해 뒀다 |
| 7 | `STORAGE_BUCKET`·`STORAGE_ENDPOINT` | prod 기동이 막힌다 — 막히는 것이 맞다 |
| 8 | MySQL 서버 `time_zone = '+09:00'` **명시** | 하루 한도(`CURDATE()`)와 생각상자 회상이 서버 OS 자정 기준이 된다. 컨테이너가 UTC 면 9시간 밀린다. JDBC 의 `serverTimezone` 은 드라이버 해석 기준일 뿐 서버를 바꾸지 않는다 |
| 9 | 한글 폰트가 jar 에 들어갔는지 | **PDF 한글이 □ 로 나간다 — 예외도 경고도 없이** |
| 10 | 청소기는 단일 인스턴스 전제 | 여러 대로 늘리려면 분산 잠금(ShedLock)이 먼저다 |
| 11 | 배포 직후 **예열** 뒤 트래픽 개방 | 커넥션 획득 제한(운영 3초)을 넘겨 500 이 난다. 헬스체크만으로는 풀이 안 찬다 |
| 12 | **파일 저장소 백업** — DB 덤프와 같은 시각 일 1회 · 30일 보관 | DB 만 되돌리면 `downloadUrl` 이 가리키는 실물이 없다 |

```bash
# 11 예열 — 트래픽 붙이기 전에
for i in $(seq 1 30); do curl -s -o /dev/null https://<도메인>/api/courses; done
```

### 2-3. 배포 전 필수 — 전부 닫혔다 (2026-09-07)

| 무엇 | 어떻게 닫았나 |
|---|---|
| 폰트 OFL 라이선스 | `src/main/resources/fonts/OFL.txt` 동봉 · jar 포함 확인 |
| `refresh_token` 색인 | `idx_refresh_token_expires` — EXPLAIN 이 `ALL`+filesort 에서 `range` 로 |
| 시더 실행법 | 루트 `README.md` + `정리.md` §2-0 · 복사본 리허설 7분 19초 |

---

## 3. 되돌리기 — 백업과 복구

```bash
mysqldump -u<user> -p --single-transaction --routines --triggers --events \
  --set-gtid-purged=OFF --default-character-set=utf8mb4 <db> > backup.sql

mysql -u<user> -p -e "CREATE DATABASE <새 스키마> DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;"
mysql -u<user> -p --default-character-set=utf8mb4 <새 스키마> < backup.sql
```

실측: 덤프 0.17 MB·1초 미만 · 복원 1.4초 · 복원본으로 `run-all.sh` 를 돌려 원본과 요청·단언·채점이 모두 같았다.

**덤프에는 전자책·인증서 PDF·사진이 없다.** 파일 저장소를 **같은 시각에 함께** 백업해야 복구가 성립한다.
시각이 어긋나면 DB 에는 있는 키의 파일이 백업에 없다. 복구 리허설에는 파일을 포함해
인증서 한 장의 `downloadUrl` 을 열어 `%PDF` 가 나오는 것까지 본다.

---

## 4. 절대 잊으면 안 되는 설계 규칙 여덟

이유를 모르면 되돌리기 쉬운 것들이다. 전부 **한 번 데인 뒤에 넣은** 규칙이다.

| # | 규칙 | 왜 |
|---|---|---|
| 1 | **일괄 UPDATE 는 PK 순 배치로 나눈다** | 조건을 훑는 UPDATE 는 보조 색인 → PRIMARY 순으로 잠그고, 단건 UPDATE 는 그 반대다. 청소기와 미션 제출이 이걸로 교착했고 **진 쪽이 사용자라 500 이 나갔다**. 고르기(잠그지 않음) → PK 로 갱신 → 200건마다 커밋 |
| 2 | **잠금은 "있는 행" 에만 건다** | `MAX(...) FOR UPDATE` 같은 빈 범위 잠금은 갭 잠금이라 서로 배타적이지 않다. 6스레드가 전부 교착했다. 사이트 행이나 기본 원고 행처럼 **존재하는 행**을 잠근다 |
| 3 | **잠금 순서를 한 줄로 정해 두고 어디서나 그 순서로** | `users` → `pilgrimage` → `stamp`. 한 곳만 순서를 바꿔도 교착이 난다 |
| 4 | **회수는 삭제가 아니다** | 남에게 보여 준 번호는 남긴다. 인증서는 `REVOKED` 로 두고 행을 지우지 않는다. 진위 확인은 404 가 아니라 **200 + REVOKED** |
| 5 | **번호는 세어서 만들지 않는다** | `COUNT(*)+1` 은 동시 발행에서 겹친다. `cert_serial` 표가 다음 번호를 들고 있다 |
| 6 | **되돌릴 때 "이미 움직인 것" 을 함께 되돌리지 말 것** | 발송된 보상·인쇄된 책은 `needs_review` 만 켜고 사람에게 넘긴다 |
| 7 | **MySQL 유니크는 NULL 을 서로 다른 값으로 본다** | 기본 원고(`site_id NULL`)가 몇 편이든 들어갔다. 생성 컬럼 `site_key = IFNULL(site_id,0)` 로 막았다 |
| 8 | **일괄 반입은 행 단위로 검사하고 파일 단위로 넣는다** | CSV 는 한 행이라도 틀리면 0건이 들어간다. 절반만 들어간 원고는 되돌리기가 더 비싸다 |

전문과 사례는 `backend/docs/정리.md` §6 에 스물넷 있다.

---

## 5. 운영 절차 — API 로 만들지 않은 것들

### 편집자 역할

```sql
UPDATE users SET role = 'EDITOR' WHERE email = '…';   -- 부여
UPDATE users SET role = 'USER'   WHERE email = '…';   -- 회수
```

API 로 열면 "누가 편집자를 임명하는가" 라는 문이 하나 더 생기고, 관리자 계정 하나가 뚫리면 콘텐츠 전체가 바뀐다.

### 사람이 봐야 하는 것

```sql
SELECT * FROM user_reward WHERE needs_review = 1;   -- 이미 나간 보상을 되돌린 자국
SELECT * FROM print_order WHERE needs_review = 1;   -- 이미 찍는 중인 주문
SELECT file_key, retry_count, last_error FROM storage_orphan WHERE status = 'FAILED';
UPDATE storage_orphan SET status='PENDING', retry_count=0 WHERE storage_orphan_id = ?;
```

`needs_review` 는 자동으로 꺼지지 않는다. 담당자가 실물을 확인하고 내린다.

### 청소기 수동 실행

`POST /api/admin/housekeeping/run` (ADMIN) — 5분을 기다리지 않고 한 바퀴 돌린다.
응답과 로그에 작업별 **처리 건수**가 같이 실린다.

---

## 6. 검증을 다시 돌리는 법

```bash
bash backend/docs/verify/all.sh                      # 한 벌 전부(newman 2회·DB·보안·교착) — 6분
bash backend/docs/verify/db-check.sh all             # H1~H18 (H3 은 10분 실대기)
bash backend/docs/verify/security-check.sh app.log   # S1~S25
bash backend/docs/verify/load/deadlock-repro.sh      # 교착 0 · 5xx 0
node backend/docs/verify/endpoint-scan.js            # 엔드포인트 91 개수 단언
node backend/docs/verify/open-endpoint-probe.js      # 비로그인 91개 실측
```

**H12 를 3/3 실측하려면** 정리 → newman → `db-check.sh after` → 채점 → 정리 순서로 돌린다.
`run-all.sh` 뒤에 돌리면 복제할 원본이 없어 두 항목이 ➖ 가 된다.

**느린 질의를 재려면** 로그를 켠 **다음에** 앱을 기동한다. `long_query_time` 은 접속할 때의 값을 물고 있어
이미 열린 커넥션 풀은 옛 값을 쓴다.

**교착 재현 시험은 대상이 있어야 의미가 있다.** 만료 대상이 없으면 "교착 0" 이 나오는데
그것은 통과가 아니라 재지 못한 것이다. 스크립트가 대상을 먼저 만들도록 해 뒀다.

---

## 7. 숫자 — 이 값에서 벗어나면 무언가 바뀐 것이다

```
표          36개
엔드포인트  91개 (expected-endpoints.txt 한 줄이 정본)
기능        58개
JUnit       266
newman      정의 492요청 / 실행 515요청 · 단언 2,388 실행
시드        사찰 110 · 코스 12(전부 DRAFT) · 자리 60 · 후보 213 · 요소 342 · 원고 10 · 편집자 1
부하        동시 20 · 3분 → 66,584요청 · 평균 54ms · 5xx 0
```

엔드포인트 수가 달라지면 `expected-endpoints.txt` 를 함께 고쳐야 한다.
고치지 않으면 스캔·프로브·문서 셋이 어긋나 **도구가 종료코드 1 로 멈춘다** — 일부러 그렇게 해 뒀다.
분모가 조용히 줄어드는 것이 이 도구들의 단골 고장이었다.

---

## 8. 계정

| 무엇 | 값 |
|---|---|
| 관리자 | `admin@templestamp.local` / `Admin1234!` |
| 편집자 | `editor@templestamp.local` / `Test1234!` |
| 개발 DB | `temple_stamp_project` · root |

시크릿(JWT·QR·DB 비밀번호)은 `.env.local` 에만 있고 저장소에 없다. **이 문서에도 적지 않는다.**

---

## 9. 더 볼 곳

| 무엇 | 어디 |
|---|---|
| 기준 문서(가장 두껍다) | `backend/docs/정리.md` |
| 인수인계 한 장 | `backend/docs/audit/인수인계_temple-stamp_최종.md` |
| 프론트 전달 목록 | `backend/docs/audit/frontend-handoff.md` |
| 남은 일 34건 | `backend/docs/audit/audit-G-잔여작업_노션용.md` |
| 최종 점검 결과 | `backend/docs/audit/verify-ch0-9-결과_노션용.md` |
| 손으로 확인하는 10단계 | `postman/예성-손확인.md` |
| 이번 정리 전체 | `종합정리.md` |
| 기획 명세(작업 중 도착) | `backend/docs/spec/` 5개 — 감사 H 의 정본 |
