# 전부합쳐 — 권장·질의·결정요청·의견·중요보고 한 곳에

작성 2026-09-08 · 대상 회차: **챕터 11 → 11-A0 → C1 → 11-A(STEP 1~6) → C1 보강**
이 문서 하나만 읽어도 **지금 무엇이 걸려 있고, 무엇을 정해 줘야 하고, 무엇을 권하는지** 다 알 수 있게 썼다.

> 읽는 순서 — §1 지금 당장 결정이 필요한 것 → §2 찾은 결함 → §3 권장 → §4 이미 결정된 것 → §5 남은 작업.

---

## 0. 한 줄 현황

```
챕터 11      닫힘 (커밋 5853937) — 항목 14/14 · Y 15/15 · 매칭율 93%
11-A0        닫힘 (a510106) — 읽기 전용 사전 확인
C1           닫힘 (57aac91) — 사찰 조사자료 반영 · 시드 보강
11-A STEP1~6 닫힘 (5a2007d) — 엔드포인트 91 → 92
C1 보강      닫힘 — mealAvailable 값 체계 변경 · 중단 복구 · 빈 DB 차이 0
11-A STEP7~10  대기 (유일성·회향 재정의)
11-B         대기 (CORS 패턴)
챕터 12      대기 (최종 게이트)
```

전 구간 `all.sh` 초록 · JUnit **305/305** · newman 2,503+175 단언 · 보안 86/0 · 교착 0 · 5xx 0.

---

## 1. 🚨 지금 당장 결정이 필요한 것 — 넷

### 1-1. 같은 절이 두 site_id 로 두 ACTIVE 코스에 들어가 있다 (가장 급함)

11-A STEP 6 이 12코스를 열면서 **실제 위험이 됐다.**

| 절 | site_id | 코스 | 자리 | 구 |
|---|---:|---|---:|---:|
| 조계사 | 1 | 1 서울 도심 다섯 절 (ACTIVE) | 1 | **1** |
| 조계사 | **990477** | 46 서울 공양의 길 (**ACTIVE**) | 5 | **5** |
| 진관사 | 5 | 1 서울 도심 다섯 절 (ACTIVE) | 5 | **5** |
| 진관사 | **990470** | 46 서울 공양의 길 (**ACTIVE**) | 1 | **1** |
| 화계사 | 3 / **990472** | 코스 1 / (자리 없음·후보만) | | |
| 도선사 | 4 / **990478** | 코스 1 / (자리 없음·후보만) | | |

**한 사람이 조계사에서 도장을 두 번, 진관사에서 두 번 받을 수 있다.**

막지 못하는 이유가 둘이다.

- 챕터 11 의 `uk_slot_site_site (site_id, track)` — **site_id 가 다르다.**
- 11-A STEP 7-2 가 넣을 `(user_id, region_id, verse_no)` — **verse_no 가 1 과 5 로 다르다.**

> **유일성을 넣어도 이 구멍은 남는다.** 중복 등록 자체를 정리해야 한다.

**권장 — ②안.** 시드 행 넷(990477·990470·990472·990478)을 지우고 코스 46 의 두 자리를 다른 후보로 채운다.
①안(데모 5곳 제거)은 **도장 1,620건이 FK RESTRICT 로 걸려** 있어 위험하다.

지시받은 대로 **11-A STEP 6-5 에서 H19(ACTIVE 코스 간 중복 사찰 0) 신설과 함께** 처리하겠다.

---

### 1-2. 관리자 코스 자리 수정이 죽어 있다 — 대구/경북 코스가 안 열린다

```
PUT /api/admin/courses/39  (3구 대표를 고운사 → 희방사)
→ 500 COMMON-5000

Caused by: SQLIntegrityConstraintViolationException: Cannot delete or update a parent row:
  a foreign key constraint fails (`slot_site`, CONSTRAINT `fk_slot_site_course_site_id`
  FOREIGN KEY (`course_site_id`) REFERENCES `course_site` (`course_site_id`) ON DELETE RESTRICT)
```

`courseService.save` 가 자리를 바꿀 때 `course_site` 를 **지우고 다시 넣는다.** v4 에서 `slot_site` 가
생기며 그 행들이 `course_site_id` 를 RESTRICT 로 잡게 됐고, **후보가 하나라도 붙은 코스는 자리를 영영 바꿀 수 없다.**
지금 모든 코스에 후보가 붙어 있다.

겹치는 문제가 둘이다.

1. **기능** — `정리.md` §3 의 "관리자 콘텐츠 저장은 UPSERT 만 · 삭제 후 재삽입 금지" 를 이 경로가 어긴다.
2. **오류 처리** — FK 위반이 `COMMON-5000` 으로 새어 나간다. 관리자에게 왜 막혔는지 전달되지 않는다.

**당장의 위험은 없다** — 고운사가 INACTIVE 라 그 코스는 `ADMIN-4092` 로 어차피 안 열린다.
지시대로 **11-A STEP 7 에서 함께 고치겠다.**

---

### 1-3. QR 위치 안내를 "미정" 문구로 채워 열었다 — 이 문구로 괜찮은가

사찰 108곳을 ACTIVE 로 올리려는데 **전부 `ADMIN-4092`** 로 막혔다. 원인은 `qr_location_hint` 가 비어 있어서이고,
그것은 **시더의 의도**였다.

```java
// SiteSeedImporter — qrLocationHint 는 현장에서 정한다 → null → ACTIVE 로 못 올라간다(의도).
```

"대웅전 앞 안내판 우측" 같은 문구를 108곳에 지어 넣으면 **현장 실사 결과인 척**한다. 그래서 넣은 것은
**미정임을 그대로 말하는 한 줄**이다.

```
QR 위치 미정 — 종무소에 문의 (현장 실사 전, 2026-09)
```

- 화면에 그대로 나간다. 거짓은 아니다.
- **이 문구가 남은 사찰 수가 곧 미실사 절의 수**이므로 진행 상황이 저절로 보인다.
- 바꾸려면 `backend/docs/verify/qr-hint-placeholder.js` 의 한 줄만 고치면 된다.

**질의 — 이 문구를 그대로 쓸지, 다른 표현을 쓸지.** 지시하신 "QR 미실사 뱃지" 는 11-A STEP 6-5 에서 함께 하겠다.

---

### 1-4. `all.sh` 를 돌릴 때마다 12코스가 닫힌다

11-A STEP 6 이 올려 둔 ACTIVE 코스 12개가 다음 `all.sh` 뒤 **1** 로 돌아가 있었다. 되돌리는 곳이 둘이다.

```sql
cleanup.sql:155          UPDATE course SET status='DRAFT' WHERE course_id BETWEEN 37 AND 48 AND status='ACTIVE';
w-hoehyang-teardown.sql  UPDATE course SET status='DRAFT' WHERE course_id <> 1;
```

둘 다 **"ACTIVE 코스가 1개인 것이 정상"** 이던 시절의 장치다. 회향 시험(W2)이 12코스를 잠시 올렸다가
되돌리는 흐름이었고 `cleanup` 은 그 실패를 대비한 안전망이었다.

**이번에 고치지 않았다.** `cleanup` 안전망 · W2 setup/teardown · **회향 정의**가 한 덩이이고,
11-A STEP 7-3 이 회향을 "권역별 1코스" 로 재정의하면 W2 전제가 통째로 바뀌기 때문이다.
지금 `cleanup` 만 고치면 회향 시험이 깨진다. **11-A STEP 7-3 에서 셋을 함께 고치겠다.**

---

## 2. 이번 회차들이 찾아낸 결함

### 2-1. 사진 바이트가 한 장도 저장된 적이 없었다 (고침 — 11-A STEP 4)

| 확인 | 결과 |
|---|---|
| presign 이 주는 `uploadUrl` | `http://localhost:9000/...` |
| `localhost:9000` | **연결 실패** — 그 자리에 서버가 없다 |
| 저장소 `PHOTO/` | **파일 0개** (`EBOOK/` 2 · `CERT/` 1 은 서버가 직접 쓴 것) |

전자책 조판이 사진을 못 찾으면 "(사진 없음)" 으로 넘어가도록 만들어 둔 것이 **이 사실을 덮고 있었다.**

**고침** — `PUT /api/uploads/**` 신설(엔드포인트 91→92). 1.2MB → 201 → 디스크 1,228,800 바이트 실재 ·
3MB → 413 · 남의 키/서명 변조 → 400. **`sign()` 을 처음으로 검증 쪽에도 쓴다.**
사진을 못 찾으면 이제 **WARN 한 줄 + 완성 로그에 "사진 누락 n"** 이 남는다.

### 2-2. QR 이 URL 이 아니라 순수 토큰이었다 (고침 — 11-A STEP 3)

`issueQr` 은 초기 커밋 이후 한 번도 바뀌지 않았고 토큰만 굽고 있었다. 그런데
**프론트 설계(`frontend-design.md` §2)에는 이미 `/checkin?token=` 라우트가 있었다** — 설계가 앞서 있었다.

기본 카메라로 찍으면 `djF8MXwx…` 라는 글자만 보여 주고 끝난다. 앱을 깔지 않은 사람은 무엇을 할지 알 수 없다.
**고침** — `{FRONTEND_URL}/checkin?token=…` 을 굽는다. 서버가 받는 것은 여전히 토큰만이다.

### 2-3. 타입 생성기가 중첩 record 를 통째로 놓쳤다 (고침 — 11-A STEP 1)

`match` 는 첫 하나만 잡는다. `SitePageResponse` 는 안에 record 가 **9개**, `PassportResponse` 는 **5개**인데
각각 하나만 나왔고, 결과물에 **선언 없이 참조되는 이름**이 남아 **`tsc` 가 거절하는 파일**이었다.

`error.fields` 도 틀려 있었다 — `Record<string,string>` 으로 적혀 있었지만 실제는 `FieldError[] {field, reason}`.

**고침** — `matchAll` · Lombok `@Getter` DTO 도 수집 · DTO 104 → 182 · 미선언 타입 **0** · **`tsc --noEmit` 통과**.
`.d.ts` 는 초기값을 담을 수 없어(TS1039) 엔드포인트 값 목록을 `api-endpoints.ts` 로 분리했다.

### 2-4. 생성 컬럼이 개인 소장본을 사용자당 한 권으로 묶었다 (챕터 11 이 만들고 고침)

`ebook.milestone_key` 를 `IFNULL(milestone,0)` 으로 두자 마일스톤 없는 **모든** 책이 0 으로 접혀
`UNIQUE (user_id, ebook_type, milestone_key)` 가 개인 소장본을 한 권으로 묶었다.

```sql
-- 고친 뒤 — 전자일기장에만 값이 있다. 나머지는 NULL 이라 제약이 걸리지 않는다.
milestone_key SMALLINT GENERATED ALWAYS AS
    (CASE WHEN ebook_type = 'INTERIM' THEN IFNULL(milestone, 0) END) STORED
```

같은 수법이 두 표에서 **정반대 방향**으로 쓰인다 — `user_reward` 는 NULL 을 접어 제약을 **걸려고**,
`ebook` 은 NULL 을 남겨 제약을 **걸지 않으려고**.

### 2-5. `ON DUPLICATE KEY UPDATE` 가 새 유니크를 만나면 남의 행을 고친다 (챕터 11)

`slot_site` 에 유니크를 걸자 `upsert` 가 다른 자리의 행을 **조용히 고치고 성공을 돌려주게** 됐다.
`SlotSiteService.assign` 한 곳으로 쓰기를 모아 주인을 먼저 확인하고 409 를 낸다.

> **유니크를 새로 걸 때는 그 표를 UPSERT 하는 문장을 전부 다시 읽어야 한다.**

### 2-6. 프로세스 누수 — `TaskStop` 이 자식 JVM 을 남긴다 (C1 보강에서 발견)

빈 DB 재생성용 앱을 멈췄는데 gradle 래퍼만 죽고 **자식 JVM 이 살아남아** `temple_stamp_c1` 에
Hikari 풀을 붙들고 있었다(6시간). 앞서 본 "Hikari 풀 고갈" 의 배경이 이것이다.

**앞선 풀 고갈은 누수가 아니라 자원 압박으로 결론.** 프로세스를 정리하고 JVM 하나로 띄우자
`all.sh` **2회 연속 초록**(교착 0 · 5xx 0)이다. 이제 `bootJar` + `java -jar` 로만 띄우고,
시더처럼 잠깐 쓰는 JVM 은 **쓰고 나서 바로 죽인다.**

### 2-7. 낡은 검사 둘 (검사가 거짓을 말하고 있었다)

| 검사 | 무엇이 틀렸나 | 처리 |
|---|---|---|
| 보안 S12 | `ThinkboxMapper` 의 **공개 목록** 질의를 보면서 "전자책 수록 질의가 is_private = 0 만 고른다" 라고 적혀 있었다. 챕터 9 결정은 전자책에 비공개도 싣는 것이라, 그대로 두면 **틀린 사실이 매 실행 초록으로 찍힌다** | 고침(챕터 11) — 보안 85 → 86 |
| 보안 S24 | "만료 뒤 거부는 측정 대상이 없다(서명 검증 주체 부재)" — **11-A STEP 4 가 그 주체를 만들었다** | 11-A STEP 10 에서 고칠 예정 |

### 2-10. 돌아가는 jar 를 덮으면 앱이 **조용히** 망가진다 (C1 보강 마무리에서 발견)

`all.sh` 가 newman 1회차에서 46 단언 실패로 멈췄다. 무너진 곳은 전자책 조판이었다.

```
java.lang.IllegalStateException: 한글 폰트를 읽지 못했다: fonts/NotoSansKR-Regular.ttf
Caused by: java.util.zip.ZipException: invalid stored block lengths
```

폰트 파일은 멀쩡했다. **jar 가 앱 밑에서 바뀐 것**이다 —
앱은 04:30 에 떴는데 그 뒤 `bootJar` 가 04:58 에 같은 파일을 덮었다.
JVM 은 jar 를 연 채로 들고 있어서, **그때까지 안 읽은 자원**을 읽으려는 순간 처음 터진다.

**이 결함의 성질이 고약하다.**

- 기동은 성공한다. 이미 읽은 클래스는 멀쩡히 돈다.
- **몇십 분 뒤, 그 자원을 처음 쓰는 요청에서만** 터진다.
- 터진 곳(전자책 조판)과 원인(jar 덮어쓰기)이 아무 관련이 없어 보인다.

앱을 내리고 다시 구운 뒤 띄우자 그대로 통과했다.

> **규칙** — `bootJar` 는 **앱을 내린 뒤에** 돌린다. `./gradlew test` 도 `processResources` 를 건드리므로
> 앱이 떠 있는 동안에는 돌리지 않는 편이 안전하다. §2-6 의 프로세스 누수와 함께
> "JVM 하나 · 굽고 나서 띄운다" 를 지킨다.

### 2-9. JUnit 이 11-A 를 따라오지 못하고 있었다 (C1 보강 마무리에서 발견 · 고침)

`all.sh` 는 초록인데 `./gradlew test` 가 **컴파일부터 실패**했다. 셋이 겹쳐 있었다.

| 무엇 | 왜 |
|---|---|
| `JwtProviderTest` · `QrTokenProviderTest` **컴파일 실패** | 11-A STEP 3 이 `AppProperties` 에 `frontendUrl` 을 넣었는데 두 시험 fixture 가 인자 셋 그대로였다 |
| `RecordIntegrationTest` **4건 400** | 11-A STEP 4 가 "저장소에 그 파일이 있는가" 를 보게 했는데, 시험은 키를 **지어내기만** 했다 |
| `SeedConsistencyTest` FLOWER **5건** | C1 보강의 CSV 재작성이 `flower_badge` 다섯 칸을 지웠다 |

**두 번째가 이 셋 중 가장 뼈아프다.** 시험이 키를 지어내기만 했기 때문에
*"사진이 한 장도 저장된 적이 없다"* 는 사실이 **시험에도 걸리지 않았다.**
이제 시험이 프론트가 밟는 길(presign → PUT → 제출)을 그대로 밟는다.

세 번째는 **공양원문 68행을 잃을 뻔한 것과 똑같은 사고**다 — CSV 를 통째로 다시 쓰는 도구가
건드릴 이유가 없는 칸까지 비웠다. 되살린 다섯은 불갑사 꽃무릇 · 향일암 동백 · 영평사 구절초 ·
반야사 배롱나무 · 봉녕사 매화·목련이다. (나머지 32칸은 `미확인` → 빈칸이라 둘 다 null 로 읽혀 해가 없다.)

> **권장** — CSV 를 다시 쓰는 도구는 **자기가 담당하는 칸만** 건드리게 하고,
> 끝난 뒤 HEAD 와 칸 단위로 대조하는 한 줄을 붙인다. 이번에 그 대조로 셋 다 찾았다.

### 2-8. 시크릿 검사에 구멍 — 단독 줄의 base64 를 놓친다

QR 토큰 전문을 문서에 실었을 때, **URL 안에 있으면** 걸리고 **단독 줄이면** 안 걸렸다.
문서에서 토큰을 지우고 해당 사찰 QR 을 **회전**시켰다(버전 2). 검사 규칙 보강은 남은 일이다.

---

## 3. 권장사항

| # | 무엇 | 왜 |
|---|---|---|
| 1 | **중복 등록 정리를 FE-1 전에** | §1-1. 유일성으로 못 막는 구멍이다 |
| 2 | **`PUT /api/admin/courses` 를 UPSERT 로** | §1-2. 지금은 코스 자리를 아무도 못 바꾼다. FK 위반이 500 으로 새는 것도 함께 |
| 3 | **뷰포인트·뱃지 삭제 API** | 지금 `POST` 만 있어 잘못 넣은 것을 지울 길이 없다. C1 에서 비공식 뱃지를 못 지워 CSV 를 되돌려야 했다 |
| 4 | **`site.description` 은 `site_i18n(ko)` 로 충분** | 새 컬럼을 만들지 않았다. ko 행이 115개 있고 105개에 소개가 있다 |
| 5 | **`api-types.d.ts` 의 참조 타입 null 표시** | `SlotBlock.extPhrase` 가 non-null 로 나오는데 실제로는 null 이 온다. 프론트가 `slot.extPhrase.title` 을 그냥 읽으면 터진다. 생성기가 알려면 `@Nullable` 표시가 코드에 있어야 한다 |
| 6 | **봉인사 좌표는 사람이 지도에서** | 카카오에 등록이 없다. 주소 검색 0건, 이름 검색은 진주 봉인사·서울 용산 유물이 잡힌다. **반경 150m 안에서만 도장이 나가므로 틀린 좌표는 없는 좌표보다 나쁘다** |
| 7 | **봉은사가 조사 자료에 없다** | 데모 코스의 절인데 `sites-master.csv` 에 행이 없어 `site-enrich.csv`·`credits.json` 둘 다 빠졌다 |
| 8 | **부하 시험은 머신이 조용할 때** | §2-6. 예열 뒤에 돌린다(`정리.md` §5-6) |
| 9 | **`sites-master.csv` 가 두 벌이다** | `backend/docs/content/` 판은 ch0~10 커밋 이후 손대지 않았고, 실제로 쓰이는 것은 `src/main/resources/seed/` 판이다. 두 벌이 있으면 언젠가 틀린 쪽을 고친다 — 한 벌로 줄이거나, 문서 판을 "원본 보관" 이라고 이름에 적자 |
| 10 | **CSV 를 다시 쓰는 도구는 자기 칸만** | §2-9. 공양원문 68행과 flower_badge 5칸을 같은 방식으로 잃을 뻔했다. 도구 끝에 HEAD 와 칸 단위 대조 한 줄을 붙인다 |
| 11 | **`app.log` 가 526MB 다** | 로컬 `logging.level.com.templestamp: debug` 라 실행 SQL 이 전부 쌓인다. **커밋되지는 않고**(`.gitignore` `*.log`) **운영은 이미 info** 다 — 남은 것은 디스크뿐이니 회전(크기 상한)을 걸거나 `all.sh` 전에 비우자 |

---

## 4. 이미 결정된 것 (되돌리려면 말씀만)

| 항목 | 결정 | 근거 |
|---|---|---|
| **사은품** | 3코스마다 전자일기장(DIGITAL) · 12코스 완주에 맞춤형 특별앨범(PHYSICAL) **둘만**. 도장·쿠폰 정책은 `is_active=0`(행은 남긴다) | 챕터 11 결정 A |
| **전자일기장 회수** | 하지 않는다 — 이미 읽은 책을 뺏을 수 없다 | 결정 C |
| **이동시간** | 기본 15분 · **사용자 단위**(코스를 갈아타는 순간이동도 잡는다) | 결정 D·E |
| **정확도** | HIGH ≤30m · MID ≤100m · LOW. **프론트가 판정**(서버는 좌표를 안 받는다) | 결정 F |
| **`mealAvailable`** | `TEMPLE_MEAL`(이름난 6곳) · **`RESERVATION`(기본값)** · `NONE`(확인된 곳만) · `NEARBY`(확인된 곳만). **NULL·빈칸 0** | C1 보강 · 2026-09-08 |
| **공양 문구** | 값만 보고 "공양 가능" 이라 띄우면 안 된다. **"공양 가능 — 방문 전 종무소에 시간·예약 문의"** 를 함께 보여 준다 | 〃 |
| **동명이찰** | `site.name` 은 **건드리지 않는다.** 구분은 **표시 계층에서만** — 프론트가 `name(시군구)` 로 그린다 | C1 보강 결정 |
| **의성 고운사** | DB 대상 제외 · INACTIVE 고정 · 대구/경북 3구 대표는 희방사 | 사찰조사 §0-2 |

**동명이찰을 적용하지 않는 이유** — 한 번 적용해 봤고 **넷이 깨져서 되돌렸다**: 원고 CSV 반입(이름 완전 일치라 0건) ·
newman N 폴더 8건 · `ManuscriptImportTest` · `SeedConsistencyTest`.

---

## 5. 남은 작업

| 회차 | 내용 | 상태 |
|---|---|---|
| **C1 보강** | mealAvailable 값 체계 · 동명이찰 목록 · 복구 | **마무리 중**(H17·JUnit·all.sh) |
| **11-A STEP 6-5** | 중복 시드 정리 + **H19 신설** · QR 미실사 뱃지 | 대기 |
| **11-A STEP 7** | 유일성 `(user, region, verse)` · **회향 재정의**(권역별 1코스) · 코스 PUT 500 수정 · 3조건 실측 | 대기 |
| **11-A STEP 8~10** | 기능 63 총점검 · cleanup q-% · all.sh 92 · 문서·커밋 | 대기 |
| **11-B** | CORS 사설 IP 패턴 · prod 금지 · preflight 6건 | 대기 |
| **챕터 12** | 최종 게이트 — DB H1~H19 · all.sh 2회 · 보안 수동 6 · 기능 63 · 부하 · 리허설 | 대기 |

### 콘텐츠 트랙(예성·별도 인력)

| 무엇 | 지금 |
|---|---|
| **사이 거리 240행** | 양식·본보기 20행 있음(`site-distance-sample.csv`). **관리자 API 는 이름→id 변환을 하지 않는다** — id 를 채워야 한다 |
| **대표 사진** | `credits.json` 뼈대 110행(값 비움) · `사진수집_지침.md`. 라이선스 넷 중 하나라도 비면 폴백 |
| **QR 부착 위치** | 108곳이 "미정" 문구. 현장 실사 |
| **원고 140편** | 양식·예시 10행(`manuscripts.csv`). dryRun → 반입 → 심사 |
| **확장문구** | 7 tier × 5구 = 35칸 전부 채워져 있으나 **AGE30 만 24편이고 나머지 6 tier 는 구마다 1편뿐**. 1편이면 "미노출 우선" 규칙이 매번 같은 글을 준다 |
| **봉인사 좌표** | 1곳 |

---

## 6. 숫자 한눈에

| | |
|---|---|
| 엔드포인트 | **92** (11-A STEP 4 가 `PUT /api/uploads/**` 하나 신설) |
| JUnit | **305 / 305** |
| newman | **2,503 + 175** 단언 · 실패 0 |
| 보안 | **86 / 0** |
| DB | H1~H18 어긋남 0 · H17 ALTER 사슬 ↔ schema.sql 차이 0 |
| ALTER 사슬 | `ch7 → ch1-withdraw → ch8 → ch9 → ch10 → ch11 → c1 → c1b` (여덟) |
| 사찰 · 코스 | **115 · 13** (빈 DB 재생성에서도 같다) |
| ACTIVE | 사찰 113 · 코스 12/13 (`all.sh` 뒤에는 1 — §1-4) |
| 공양 | `RESERVATION` 108 · `TEMPLE_MEAL` 7 · **비어있음 0** |
| 좌표 미확보 | **1**(봉인사) — 빈 DB 기준으로는 18 |
| 명세 매칭율 | **93%** |
